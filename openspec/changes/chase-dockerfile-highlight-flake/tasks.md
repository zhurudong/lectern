## 1. 分开两个假说(**先做这个,不要先猜修法**)

- [ ] 1.1 在断言点采一次**同瞬快照**:文档文本是否已出现、高亮 span 数、当前 language/legacy-mode 是否已加载完成 —— **三者同一时刻取**,不同时刻取会得出互相矛盾的结论
- [ ] 1.2 判读规则先写死,再去看数据:**语法已加载但 span 为 0 → 假说 B(产品侧)**;**语法尚未加载完成 → 假说 A(断言太早)**
- [ ] 1.3 若快照连续多轮未触发(该偶发不复现),**如实记为"未复现",MUST NOT 据此宣布产品正常** —— 未复现不是证据

## 2. 按结论处置

- [ ] 2.1 若为 A:把等待改成等"要断言的那个性质本身"(高亮 span 出现),**门槛一个不动**,等不到照常判红
- [ ] 2.2 若为 B:定位 legacy-mode 加载竞态并修复;**用户可见缺陷 MUST 有回归断言**
- [ ] 2.3 若无法定论:**如实结案**,在发版说明保留已知偶发披露,**不进隔离通道**

## 3. 边界

- [ ] 3.1 本 change **不阻塞发布**;结论落在后续维护版本(**版本号与商店状态的记载见 proposal 的待核实标注**)
- [ ] 3.2 **MUST NOT 为了让套件好看而把该项静音** —— 这是立项的全部理由
