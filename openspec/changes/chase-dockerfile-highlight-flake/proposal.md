> **这是一次根因追查,不是一个已知修法的实施。** 开工时**不知道**怎么修 —— 若追查得出"无法稳定复现且产品行为正常",**如实结案也是合格结果**,MUST NOT 为了收尾而编一个修法。

## Why

E2E 断言「`Dockerfile.dev`:专用高亮生效」**偶发失败**(高亮类 0),近 13 轮里红过 2 次。

**关键裁定:本项 MUST NOT 进隔离通道(`checkKnownFlaky`)。** 进那条通道的门槛是"有独立证据表明产品行为正常",**我们没有**。

**两个假说的用户后果完全不同,而它们尚未被分开:**

- **假说 A(断言太早)**:文本出现 ≠ 着色完成,只是断言时机问题 → **用户无感**;
- **假说 B(产品侧偶发)**:legacy-mode 语法加载偶尔没接上 → **用户打开该类文件会看到没有高亮的纯文本**。

**现有证据偏向 B。** 已按 `scss/sass` 那次的修法(等着色完成)加了等待,**加完仍红过一次**。若纯粹是断言太早,加等待就该收敛。

> **把一个可能的产品缺陷标成"已知偶发"并静音,是"入口绿了、断在入口之后"这一族的终极形态 —— 它把用户会遇到的事,变成我们不会看见的事。**

## What Changes

- 追查该偶发的根因,**先分开 A/B 两个假说**,再谈修法。
- **不阻塞发布**:本项只影响着色,不影响内容、跳转、搜索、导航;结论落在后续维护版本。发版说明按"已知偶发"如实披露,**披露不等于静音** —— 前者用户知情,后者我们自己看不见。
  > ⚠️ **一处待核实的记载(2026-08-27 核验标注)**:本节此前写有"该版本用于替换**商店中早于最终验证构建的 0.3.0 包**"。**现有核验记录无法证实商店里存在这样一个包**,也不掌握该说法的来源;改动发生在 08-26,作者不明。
  > **已改为不依赖该前提的表述** —— "不阻塞发布"无论商店状态如何都成立。**若确有其事,请补上依据(上传时间/版本/谁传的)再写回;在那之前 MUST NOT 把它当作既成事实引用。**

## Impact

- 受影响能力:file-preview(语法高亮),仅显示层。
- 无契约变更(`skip_specs: true`);若追查证明是产品侧竞态且需要新承诺,再另立带 spec delta 的 change。
