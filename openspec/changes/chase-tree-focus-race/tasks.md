## 1. 取证(**先拿到同瞬证据,再谈修法**)

- [ ] 1.1 在**能复现的负载条件**下反复跑(1.3 万文件夹具 + 索引/Worker churn),直到同瞬快照被触发至少一次
  > **轻量复现脚本已被证明不够**:40 轮真重挂仍 0 失效 —— 复现依赖负载,不是逻辑本身随机
- [ ] 1.2 快照必须**同一次求值里取齐三样**:`aria-activedescendant` 属性值、`activePath` 的实际内容、`document.activeElement === containerRef.current` 的**严格**判定
- [ ] 1.3 据此判定那一瞬 `activePath` 到底是什么 —— **"属性缺失"与"↓ 落到第二行"不可能同瞬成立,所以必有一次观察不在那一瞬**

## 2. 修复(**取证之后才做**)

- [ ] 2.1 据证据定位并修复
- [ ] 2.2 **MUST 在能复现的负载条件下反复验证**,不接受"改完那次绿了"
- [ ] 2.3 **写明这次的双向证明覆盖了哪条路径** —— 教训:上次的双向证明证明的是"该修复对那一个复现路径有效",**不等于问题不再存在**
- [ ] 2.4 修复确立后把断言从 `checkKnownFlaky` 移回 `check`

## 3. 或:如实结案

- [ ] 3.1 若判定为环境噪声 / 无法复现,**如实结案**:保留隔离、把调查过程与结论写进台账,**MUST NOT 悄悄删掉断言** —— 删掉等于把"没查明白"变成"没有问题"
