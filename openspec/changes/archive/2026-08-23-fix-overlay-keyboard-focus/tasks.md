## 1. 共享焦点管理(**三处需要,重复已发生**)

- [x] 1.1 抽出一份**临时浮层焦点管理**:打开时移入焦点并定位初始项、维护选中项、关闭时归还焦点到触发处
- [x] 1.2 **MUST NOT 各写一份** —— 候选面板、引用面板,以及 `fix-help-panel-a11y` 待做的帮助面板都用它。**这次抽象是由重复驱动的**(三处),不是预感;当初大纲不抽是因为当时只有两处且数据模型不同,现在条件变了

## 2. 多候选面板(用户报的那条)

- [x] 2.1 弹出时焦点移入,落在**第一项**
- [x] 2.2 `↑↓` 改变选中项,**选中项必须可见**(超出视口要滚回来)
- [x] 2.3 `Enter` 跳转到**当前选中项**(不是永远第一项)
- [x] 2.4 `Esc` 关闭并**把焦点归还到代码区原位**,用户可接着按键
- [x] 2.5 鼠标行为不变(点击仍可选)

## 3. 引用面板(**同源、且更彻底**)

- [x] 3.1 结果行改为**键盘可达**(现在是 `<div>` + `onClick`,不可聚焦)
- [x] 3.2 `↑↓` 在结果间移动、`Enter` 跳转到该处、`Esc` 关闭并归还焦点
- [x] 3.3 跨文件分组下的移动语义要连贯(跨组时不卡住)

## 4. 修正被高估的断言

- [x] 4.1 **修 `add-keyboard-first-navigation` 4.1 的链路断言**:现在它只验"引用面板出现""内容非空",**必须改成用键盘选中一条并完成跳转**。**这条不改,同一个洞会在下次回归里继续绿**
- [x] 4.2 顺带核 `ContentPanel`(全文搜索结果):它有 `input` 但无 keydown —— 确认结果能否键盘选中;**若同样不能,并入本 change 一起修**(同一形状不该分两次发现)

## 5. 验收

- [x] 5.1 E2E:`⌘↩` 多候选 → 断言**焦点在面板内** → `↓` 改选中(断言选中项变了)→ `Enter` **跳到第二项而非第一项**(证明选中真的生效)
- [x] 5.2 E2E:`⌘⇧↩` 引用 → 键盘选中一条 → `Enter` → 断言预览跳到该处
- [x] 5.3 E2E:两个面板 `Esc` 关闭后**焦点回到触发处**(不是"关掉了"就算过)
- [x] 5.4 **回归门禁**:既有断言全绿;`npm run build` / `npm run check` / `--all --strict` 全绿
