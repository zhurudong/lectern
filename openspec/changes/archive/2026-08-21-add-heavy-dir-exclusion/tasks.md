## 1. 单一排除判定

- [x] 1.1 在 `src/lib/` 新增**唯一一份**排除判定(名单 + 判定函数);名单至少含 `.git`、`node_modules`,建议含 `target`、`.venv`、`__pycache__`、`dist`、`build`、`.next`、`.cache`
- [x] 1.2 **三个消费者共用这一份**:目录树遍历、文件名索引 Worker、符号索引派发。**MUST NOT 各写一份名单或各自判断** —— 规则重复出现在多处实现,就是接缝的物理形态
- [x] 1.3 判定只按**目录名**匹配,不做路径深度或内容推断(简单、可预期、用户能自己解释为什么某个目录被隐藏)

## 2. 目录树:排除可见且可逆

- [x] 2.1 被排除项所在层级显示"已隐藏 N 项"行(N 为该层被排除的条目数),**MUST NOT 静默吞掉**
- [x] 2.2 该行提供**显眼的展开入口**,展开后正常显示这些目录,可继续下钻
- [x] 2.3 展开状态在会话内保持;刷新目录树后回到默认排除状态(与既有刷新语义一致)
- [x] 2.4 "已隐藏 N 项"行的样式在浅色/暗色下都清晰,且**与普通目录行可区分**(它不是一个目录,是一个提示)

## 3. 可浏览 vs 被索引

- [x] 3.1 展开被排除目录后,其中文件**可正常预览、可看单文件大纲**(单文件抽取通道不受项目索引排除影响)
- [x] 3.2 这些文件**不进项目级索引**:文件名搜索、符号索引、全文搜索均不收录
- [x] 3.3 **该边界要对用户可见**:在被排除目录内打开文件时明示"此目录不参与项目级搜索与跳转",**MUST NOT 让用户把"能打开却搜不到"当成缺陷**

## 4. 验收

- [x] 4.1 E2E:合成一个**带 `node_modules`(数千文件)与 `.git`** 的项目 —— **这是本 change 的关键夹具**,既有合成项目从不长这两样,这正是问题一直没被发现的原因
- [x] 4.2 E2E 断言:默认状态下目录树不含依赖文件、索引文件数对应项目自身、**不触发"项目过大"降级**
- [x] 4.3 E2E 断言:"已隐藏 N 项"行存在且 N 正确;展开后目录可见、可下钻
- [x] 4.4 E2E 断言:展开后打开依赖包内文件 → 预览正常 + 单文件大纲可用;同时该文件**搜不到**(文件名/符号),且界面有对应明示
- [x] 4.5 E2E 断言:三个消费者行为一致(不出现"树里没有但能搜到"或反之)
- [x] 4.6 **回归门禁**:既有全部断言全绿,尤其规模实测那组(**排除后数字会变,断言按新语义更新而不是把新行为往旧断言上凑**)
- [x] 4.7 `npm run build` 与 `--strict` 通过;双主题视觉自检(2.4 那行的样式)并附图

## 5. 与对外材料的衔接

- [ ] 5.1 通知开源会话:README 截图 **MUST 拿真实仓库拍**,不能拿合成项目拍 —— 否则第一张图就与用户打开后看到的不一致
- [ ] 5.2 若本 change 赶不上发货:README 的"边界"一节 MUST 如实写明"当前不排除 `.git` 与 `node_modules`",**MUST NOT 拍一张避开该问题的截图** —— 拿避开缺陷的截图当封面,是假绿的对外版本
  > **裁定(2026-08-21):不适用,保持不勾。** 其前提是「本 change 赶不上发货」—— 前提未成立,故本条无需执行。**"不适用"与"已完成"不是一回事**,勾上会把一件没做也不必做的事记成做过了;留空 + 注明理由,读的人才知道这是被判过的,不是漏了。
