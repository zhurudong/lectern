## 1. 修复

- [x] 1.1 目录树 rows 就绪后,若容器持有焦点(`document.activeElement` 是树容器)且 `activePath` 仍为空,补设活动行到首个可落点行
- [x] 1.2 **不放宽 `moveTo` 的跳过逻辑** —— 跳过非 node 行是正确行为,病根是"只在 `onFocus` 尝试一次、失败不重试"

## 2. 验收

- [x] 2.1 E2E:**在树尚未就绪时**让容器获得焦点(用夹具或人为延迟制造该时刻),断言 rows 就绪后活动行**非空**且落在首行
- [x] 2.2 断言**不因此破坏既有行为**:点击行仍同步活动行、`⌘K` 聚焦时树不截获按键、端点处按钮置灰
- [x] 2.3 **回归门禁**:既有全部 E2E 断言全绿(尤其键盘导航那组)
- [x] 2.4 `npm run build` 与 `openspec validate --all --strict` 通过
