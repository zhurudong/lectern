## 1. 主修:符号搜索跳转后的焦点交接

- [x] 1.1 `SearchBox.openPath` 在导航完成后把焦点交给 `.cm-content`;**编辑器可能异步挂载**,要等其就绪(而不是在 `blur()` 那一刻盲设)
- [x] 1.2 文件名搜索(`⌘K`,不带行号那条分支)同样处理 —— 它也把焦点 blur 掉了

## 2. 审计其余入口的"离开时"焦点(**先查,再决定改哪些**)

- [x] 2.1 逐个核:候选面板 Enter、引用面板 Enter、全文结果 Enter、目录树 Enter —— **落地后 `document.activeElement` 到底在哪**
- [x] 2.2 **只修确实断的**。**MUST NOT 把清单上的都改一遍** —— 给没坏的动手风险大于收益(上一轮 `SearchBox` 就是这么被正确排除的)
- [x] 2.3 目录树 Enter 打开文件时焦点**留在树上是正确的**(用户在树里连续浏览),**不要"顺手"改成跳进编辑器** —— 那会破坏一个正常的动线
- [x] 2.4 若候选/引用现在正确,**确认它是"被指定"还是"碰巧"**:`useOverlayKeyboard` 归还到触发处,而触发处恰好是编辑器。**把它变成显式指定**,否则换个触发来源就会再断一次

## 3. 验收

- [x] 3.1 E2E:`⌘⇧O` 搜索 → 键盘选中跳转 → 断言 **`document.activeElement` 在 `.cm-editor` 内**,且**按 `↓` 后代码光标真的移动了**
  > **不是"跳转发生了"** —— 那条在焦点掉 body 时同样为真,正是这个 bug 逃过既有断言的原因
- [x] 3.2 E2E:`⌘K` 文件名搜索打开文件后,同样可直接用键盘操作
- [x] 3.3 E2E:2.2 认定为"本来就对"的入口,补一条断言把它**钉住**(否则它靠巧合成立,下次改动会静默破坏)
- [x] 3.4 **回归门禁**:既有断言全绿;`npm run build` / `npm run check` / `--all --strict` 全绿
