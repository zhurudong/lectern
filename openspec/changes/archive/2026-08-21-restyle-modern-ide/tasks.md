## 1. 主题令牌替换(A 方案)

- [x] 1.1 按 design D1 令牌表替换 `src/styles.css` 中 `:root`(浅)与 `[data-theme="dark"]`(深)两套变量值 —— **只换值 + 增补令牌,MUST NOT 新建平行主题体系**(只换值 + 增补令牌,沿用既有 `:root` / `html[data-theme=dark]` 两套变量,未新建平行体系)
- [x] 1.2 补齐 **PM 摘要未列但 mockup 里有的五处**:tab 激活态、搜索框底/边、徽标配色、**当前代码行高亮 `.hot`**、大纲符号色 `.ols` —— 漏掉这些会出现"主色换了但局部还是旧灰阶"的斑驳感(五处全部落实:tab 激活态 → `--tab-active-bg/fg`(我们的对应物是搜索面板模式切换)、搜索框 `--bg-input`/`--border-input`、徽标 `--badge-bg/fg`、当前代码行 `--hot-bg`/`--hot-bar`(用在 `.cm-target-line` 与 Markdown 落点)、大纲符号色。**我核过 mockup 源码本身**,另发现表里未列的 brand / tab 非激活态 / 搜索框占位符色,一并取用)
- [x] 1.3 两套 SHALL 是**各自独立调校的完整值**,MUST NOT 用"一套色加透明度/滤镜"推导另一套(两套各自独立值;E2E 另有一条断言两套色值确实不同)
- [x] 1.4 按 WCAG 检查正文与次级文本对比度;弱文本(深 `#5a6172` / 浅 `#9aa4b6`)**仅用于分组标题等非关键信息**,MUST NOT 用于正文(实测对比度:主文本浅 12.13 / 暗 11.53、次级浅 5.76 / 暗 7.03,均过 AA 正文 4.5。弱文本 2.36/2.91 不过 AA,但**逐处核对了它的 30 个使用点**,全部是元信息/占位符/禁用态/行号,无正文。**顺带修正一处**:大纲的"暂不支持"说明与引用面板的精度边界提示原本用弱文本,但它们是 spec 要求"必须明示"的文案,用 2.36:1 会削弱"明示"本身,已提到 `--fg-dim`)
- [x] 1.5 **注释色提到 ≥ 4.5:1**(两主题,见 design D5):实测浅 2.39 / 暗 3.04。低对比注释是**编辑器**取向(那里你在写代码),**而我们是阅读器 —— 注释承载原作者意图,常是屏幕上信息密度最高的文本,把它做成最难读的元素与产品目的正好相反**。保持色相与"比正文更暗"的层次关系,改完**数值与眼睛双复核**(数字达标但观感变抢眼,同样是失败)(浅 `#a0a6b0` → `#666d7a`(2.39 → **5.07**)、暗 `#5c6370` → `#7e8695`(3.04 → **5.01**),均过 AA 正文线。**保持原色相**(218°/219°)、**仍明显暗于正文**(正文 12.13 / 11.53),层次仍是"次要但清晰"。数值与眼睛**双复核**:另截了含中英文注释的真实代码预览(`scripts/comment-{light,dark}.png`),两主题下注释清晰可读但不抢眼,与关键字/正文的层级关系未被打破)

## 2. 语法高亮色板

- [x] 2.1 `src/preview/cmTheme.ts` 两套主题的 kw / fn / st / cm / ty / pa 按 D1 令牌表更新(改为**调色板驱动**:两套主题共用同一张 tag → 角色映射表,只换 6 个色值,避免两处各写一遍导致漂移)
- [x] 2.2 **注释在两套里都是斜体** —— 这是 A 方案的一部分,不是可选装饰(`fontStyle: italic` 写在共用映射里,两套自动都是斜体)
- [x] 2.3 验证主题切换时**代码区与界面同步换套**,不出现"界面已换、代码区仍是旧配色"的中间态(E2E 断言主题切换后代码区着色跟着换:浅 `rgb(138,63,252)` → 暗 `rgb(199,146,234)`,后者正是令牌表的 `#c792ea` —— 这条同时交叉核对了 cmTheme.ts 与 styles.css 两处色值一致)

## 3. 密度与过渡

- [x] 3.1 密度令牌化:目录行高 28px、代码行高 22px、圆角 6–8px(比现状略松仍紧凑)(目录行 `ROW_HEIGHT` 24 → 28、代码行 `lineHeight: 22px`、圆角提到 6–8px 档)
- [x] 3.2 状态过渡 0.12–0.15s `ease-out`,**只加在非布局属性**(`background-color` / `box-shadow` / `color`)(0.13s ease-out,只加在 `background-color` / `box-shadow` / `color`)
- [x] 3.3 **MUST NOT 给布局属性加过渡**(width/height/位置):侧栏与大纲是拖拽调宽的,给 width 加过渡会让拖拽变成"追着鼠标滑",**手感反而更差**(E2E 断言按**判据**而非属性名单查:把 width/height/padding/margin/top/left/flex/inset/all 一整类都过一遍,且**同时看 `transitionDuration`** —— `transitionProperty` 在没有过渡时初始值就是 `all`,只看它会把"根本没过渡"误判成"给 all 加了过渡")
- [x] 3.4 验证虚拟滚动下的过渡:行是复用 DOM 节点的,快速滚动时行背景过渡可能产生**颜色拖影**;实测确认,必要时在滚动期间禁用过渡(已实测:滚动期间 `.tree` 挂 `scrolling`、行 `transition` 变 `none`,停止后恢复。断言里额外加了一条**前提检查**"确实产生了滚动位移" —— 根目录只有十来行撑不出滚动条,不检查前提的话这条会在"根本没滚动"的情况下假绿)

## 4. 字体

- [x] 4.1 等宽字体栈改为 `ui-monospace, "SF Mono", SFMono-Regular, Menlo, Consolas, "Cascadia Mono", "Liberation Mono", monospace`
- [x] 4.2 **移除 `"JetBrains Mono"`** —— 它不是任何平台的系统字体,留在栈里对绝大多数用户是空占位,只会制造"设计稿有、真机没有"的错觉(两处字体栈都已移除;仓库内仅剩说明为何移除的注释)
- [x] 4.3 界面(非代码)字体栈保持现状(`-apple-system, "PingFang SC", sans-serif`),CJK 文件名依赖它,不动
- [x] 4.4 不内置任何字体文件(理由见 design D2:是体积取舍不是合规问题,当前取舍是不值);E2E 断言无字体相关网络请求(未引入任何字体文件;零网络断言仍绿)

## 5. 焦点环并入新色板(与键盘导航的交接)

- [x] 5.1 把 `add-tree-keyboard-nav` 定的活动行焦点环取值**并入 A 色板重调**(那轮取值是按旧灰阶定的临时值)(焦点环取值并入 A 色板:浅 `#0b63c5` / 暗 `#6cb6ff`)
- [x] 5.2 **重新验证"活动行焦点环 vs 选中标识可区分"** —— A 选中行是**蓝填充 + 蓝左色条**,再叠蓝描边环 = 一行上**三个同色系强调元素**
- [x] 5.3 区分手段:**MUST NOT 再加一种颜色**(会把问题推给色相拥挤,图标那轮已吃过),让三者**在形式维度错开**(色条=细长实心 / 填充=整行底 / 焦点环=描边),必要时焦点环在选中行上改用内缩描边或提高对比度而非换色(**没有加第四种颜色**。改为形式错开:焦点环从"整行 inset 阴影"改成 `::after` 的**内缩描边**,且左侧多缩 6px 让开选中行的 2px 色条 —— 三者变成 细长实心条 / 整行填充 / 内缩描边)
- [x] 5.4 自检时**必须看"环 + 色条 + 选中底同时出现在同一行"的样子**,不是分别看 —— 这是新组合,**不在键盘那轮自检的作用域内,不能顺延结论**(专门截了**三元素同行**的图:把活动行移回选中行,让环+色条+底叠在同一行。两主题各一张(`overlap-{light,dark}-zoom.png`),确认三者仍分得开。分别看是三张合格的图,合起来才是用户真实会看到的那一行)

## 6. 验收

- [x] 6.1 更新既有**硬编码色值断言**(如"首装默认浅色 = `rgb(255,255,255)`",A 浅色底是 `#fbfcfe`):**新值 MUST 取自 D1 令牌表这个单一源**,MUST NOT 在断言里手写"看起来对"的色值,否则断言与令牌表会各自漂移、而漂移那一刻两边都还是绿的(全部硬编码主题色值已清零。断言改为**从 `--bg` 令牌读期望值**再比对,并加了 hex→rgb 换算与 `waitThemeApplied()` 辅助 —— 令牌表是单一源,断言不手写"看起来对"的色值)
- [x] 6.2 E2E 断言主题切换后**代码区着色同步换套**(取一处语法 token 的计算色,两主题下不同且各自匹配令牌表)
- [x] 6.3 E2E 零网络断言仍绿(尤其字体不外联)
- [x] 6.4 **回归门禁**:既有全部断言全绿,尤其键盘导航那 15 条与拖拽调宽(既有断言全绿,含键盘导航 15 条与两处拖拽调宽;全量 **235/235 PASS**)
- [x] 6.5 `npm run build`(含 `tsc --noEmit`)与 `--strict` 通过;确认未改交互逻辑、未碰 `src/intel/**`(`npm run build` 含 `tsc --noEmit` 通过;`--strict` 通过;`src/intel/**` 零改动;交互逻辑未改,`Tree.tsx` 只动了行高常量与滚动标记)
- [x] 6.6 **开发双主题视觉自检并附图**(放大看):逐项对照 D1 令牌表确认无遗漏(尤其 1.2 那五处),并含 5.4 的三元素同行检查(逐项对照 D1 令牌表核过,含 1.2 那五处;两主题各截图并放大看,另加三元素同行专项图)
- [x] 6.7 交 PM 跑真机 **hover / 选中 / 展开的动画手感**录屏或连拍 —— **过渡是本 change 的重点交付,静态截图给不了那半**;再由用户真人试用(**不发清单**)(**PM 已录 75 帧真机 gif;用户本人 2026-08-21 看后回复"手感还行" —— 真人试用这一环到线**)
