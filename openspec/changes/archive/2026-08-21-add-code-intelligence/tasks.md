## 1. 准备与依赖

- [x] 1.1 把 `@lezer/python`、`@lezer/java`、`@lezer/cpp`、`@lezer/go`、`@lezer/javascript` 从传递依赖提升为 `package.json` 的显式 `dependencies`(锁定当前 `node_modules` 中已装版本,不做被动升级),`npm run typecheck` 通过
- [x] 1.2 在 `src/lib/filetypes.ts` 增加"该类型是否参与代码理解"的标注:Python / Java / C / C++ / Go / JavaScript / TypeScript(含 JSX/TSX)为 `full`,Markdown 为 `outline-only`,其余为 `none`;导出供 UI 与 Worker 共用
- [x] 1.3 记录构建基线:当前 `dist` 各 chunk 大小写入 `docs/spike-code-intel-baseline.md`,作为体积增量校验的对照

## 2. 索引底座

- [x] 2.1 扩展 `src/search/indexWorker.ts`:walk 时同时回传 `FileSystemFileHandle`(批量),文件名索引行为与回包格式保持向后兼容(现有 `⌘K` 不回归)
- [x] 2.2 把 `searchStore.ts` 的局部 token 提为模块级全局 `generation`(项目切换 / 目录树刷新时 `++`),文件名索引、符号索引、全文搜索共用;过期回包统一丢弃
- [x] 2.3 新增 `src/intel/pool.ts`:Worker 池(`N = min(4, max(1, hardwareConcurrency - 1))`),支持任务入队、批量回传、按代次取消、Worker 异常上报
- [x] 2.4 新增 `src/intel/indexStore.ts`:列式索引结构(`files` / `fileMeta{size,lastModified}` / `symName` / `symKind` / `symFile` / `symLine` / `symContainer` / `byLowerName`)与写入、查询、清空接口
- [x] 2.5 索引状态信号:`building` / `indexedFiles` / `totalSymbols` / `state('idle'|'building'|'done'|'partial'|'unavailable')`,供顶部进度与降级提示订阅

## 3. 符号抽取(Worker 侧)

- [x] 3.1 新增 `src/intel/symbolWorker.ts`:接收 `{path, handle, langId, generation}`,`getFile` → 超 5 MB 上报"跳过(过大)" → 解码(复用 `fs.ts` 的 UTF-8 边界处理)→ Lezer 解析
- [x] 3.2 实现 Python 定义抽取(`ClassDefinition` / `FunctionDefinition` 下 `VariableName`、顶层 `AssignStatement` 常量),按 design.md D1 表
- [x] 3.3 实现 Java 定义抽取(`Definition` + 祖先过滤:类/接口/枚举/方法/字段/枚举常量),**排除 `FormalParameter` 下的 `Definition`**(实测修正:design.md D1 写的"祖先为…"若按祖先集合实现会误收**每一个局部变量** —— 局部变量祖先链 `Definition < VariableDeclarator < LocalVariableDeclaration < Block < MethodDeclaration < ClassBody < ClassDeclaration` 同时命中 MethodDeclaration 与 ClassDeclaration。已收紧为**直接父节点白名单**,字段与局部变量靠 `VariableDeclarator` 的父是 `FieldDeclaration` 还是 `LocalVariableDeclaration` 区分;详见 docs/spike-code-intel-baseline.md 第 2.1 节)
- [x] 3.4 实现 Go 定义抽取(`FunctionDecl`/`TypeSpec`/`ConstSpec`/`VarSpec` 下 `DefName`、`MethodDecl` 下 `FieldName`),**排除 `Parameter` 下的 `DefName`**
- [x] 3.5 实现 C/C++ 定义抽取(`StructSpecifier`/`ClassSpecifier` 的 `TypeIdentifier`、`FunctionDeclarator` 下 `Identifier`/`ScopedIdentifier` 末段、`TypeDefinition`、`FieldDeclaration`、`#define`),**排除 `ParameterDeclaration` 下 `Identifier`**(另行排除 design.md D1 未列出的 `#define SQUARE(x)` **宏形参**——它与宏名同为 `Identifier < PreprocDirective`;typedef 别名改取**最后一个** `TypeIdentifier`)
- [x] 3.6 实现 JS/TS 定义抽取(函数/类/方法/接口/类型别名/枚举/顶层变量),**排除 `ParamList` 下的 `VariableDefinition`** 与函数体内局部声明;方言按扩展名选择(ts / tsx / jsx)
- [x] 3.7 实现 Markdown 标题大纲抽取(仅大纲,不进 `byLowerName`、不参与跳转与引用)
- [x] 3.8 分批回传(每约 200 文件或 2,000 符号一批),并回传每文件的 `{size, lastModified}` 指纹
- [x] 3.9 用一组小样例逐语言自查抽取结果:定义齐全、**形参与局部变量不在结果中**、行号与实际一致(结果记入 `docs/spike-code-intel-baseline.md`)(`node scripts/check-extract.mjs`,20/20 PASS;结果与实测修正记入 docs/spike-code-intel-baseline.md 第 2 节)
- [x] 3.10 C/C++ 补收**自由函数原型**(`Declaration → FunctionDeclarator` 下的 `Identifier`),符号种类标为"声明"并与定义区分;类成员原型经 `FieldDeclaration → FieldIdentifier` 已收录,不重复收;`#define SQUARE(x)` 的宏形参 MUST 排除(与宏名同为 `Identifier < PreprocDirective`,只能靠位置区分)。裁决理由:头文件是读陌生 C/C++ 代码库查 API 的第一站,只收有函数体的定义会让纯声明头文件大纲空掉(自由函数原型经 `Identifier < FunctionDeclarator < Declaration` 收录为新增的"声明"种类;宏形参与 typedef 别名的处置见 3.5;E2E 断言 `src/util.h` 的 `compute` / `reset` 进入大纲且种类为 kind-15,形参 a/b 未被收录)

## 4. 文件大纲与能力明示

- [x] 4.1 新增 `src/intel/OutlinePanel.tsx`:按出现顺序(含层级)列出当前文件符号,显示种类标识与名称,点击定位到行
- [x] 4.2 大纲面板可折叠/隐藏,折叠态持久化到 localStorage(沿用侧栏宽度的 key 命名风格)
- [x] 4.3 大纲随预览文件切换同步更新,不残留上一个文件的条目
- [x] 4.4 不支持代码理解的语言在大纲区展示明确说明(非空白);Markdown 展示标题大纲
- [x] 4.5 预览区 header 增加能力标识:语言名 + "可跳转 / 仅高亮" 徽标 + 索引状态(构建中及进度 / 已完成 / 部分完成 / 不可用)
- [x] 4.6 **修 Markdown 富文本视图下大纲点击无反应**(用户 2026-08-18 真机实测发现,**违反已批准 spec**:「支持范围与能力明示」的 Scenario 写了"可点击定位",`MarkdownView.tsx` 却完全不处理行号,`targetLine` 只有 `CodeView` 认)。产品行为**取方案 A**:富文本模式下点标题 → 滚动到**渲染后的对应标题元素**并短暂标识、**停留在富文本视图**;不取方案 B(点击即切源码视图),理由:用户读 README 时被强行踢到源码是体验断裂,而大纲的价值恰恰在于"边读渲染稿边跳章节"。实现口径见 design.md D3(markdown-it `token.map[0]` → `data-cv-line`,净化侧沿用 `ADD_ATTR` 既有先例)(按方案 A 实现:渲染阶段用 markdown-it 的 `token.map[0]` 给标题打 `data-cv-line`,`PURIFY_CONFIG.ADD_ATTR` 沿用 `data-cv-path` 的先例追加该属性;大纲点击时取"不超过目标行的最近一个"元素 `scrollIntoView` + 短暂标识,**停留在富文本视图不切走**。实现中修正一处会在运行期炸掉的写法:markdown-it 的 rule 签名是 `(tokens, idx, options, env, self)`,少写一个参数会把 `env` 当成 `self`,而 TS 因该参数是 any 并不报错)

## 5. 全局符号搜索

- [x] 5.1 实现全局符号搜索 UI:输入按符号名子串匹配(不区分大小写),结果显示符号名、种类、所属容器、文件相对路径。**实现位置经方案会话裁决为 `src/search/SearchBox.tsx` 内的符号模式子组件**,不新建独立文件 —— 理由:5.2 要求界面入口是"搜索面板内的模式切换",拆成独立面板要复制一套输入框,两个输入框并存反而让"⌘K 范围恒为仅文件名"更难保证
- [x] 5.2 提供**不依赖快捷键的界面入口**(搜索面板"符号"模式切换),与 `⌘K` 在界面上明确区分,两者搜索范围互不改变
- [x] 5.3 键位绑定(**原文"改绑备用键位"已被 2026-08-17 修订口径取代**):macOS 绑定 `⌘⇧O` 并 `preventDefault`;Windows/Linux 的 `Ctrl+Shift+O` 是 Chrome 书签管理器保留键,**MUST NOT 绑定也不提示**,该平台符号搜索仅经界面入口提供(见 spec「全局符号搜索」与任务 12.5)
- [x] 5.4 选中结果打开文件并定位到符号行,同步目录树选中态(复用 `revealPath`)
- [x] 5.5 索引未就绪时提示"索引构建中"并返回已索引部分结果,不卡顿不报错

## 6. 按行定位、跳转到定义与导航栈

- [x] 6.1 `src/state.ts` 的 `SelectedFile` 增加可选 `targetLine`;新增"当前文件内定位"通道(不重读文件、只滚动)(**提前落地**:任务 4.1「点击大纲定位到行」依赖此通道,故在阶段一实现;`targetLine` 信号与 `selectedFile` 分开,同文件内定位只滚动不重读)
- [x] 6.2 `src/preview/CodeView.tsx` 实现按行定位:滚动到目标行(尽量居中)+ 短暂行高亮;目标行超出文件行数或落在截断范围外时定位到最近位置并提示(**提前落地**,同 6.1;越界/截断范围外时钳位到最近位置并给出提示)
- [x] 6.3 实现跳转主入口(两条,均不依赖浏览器保留键):`Cmd/Ctrl` + 点击标识符,以及预览区右键菜单"跳转到定义";`F12` 仅作可选补充键位,**MUST NOT 作为唯一入口**(F12 是 Chrome DevTools 保留键,页面无法可靠阻止)(两条主入口均已实现并经 E2E:⌘/Ctrl+点击标识符、预览区右键菜单。F12 未绑定 —— 它是 Chrome DevTools 的浏览器级保留键)
- [x] 6.4 跳转查询:`byLowerName` 精确名匹配 → 唯一命中直接跳转;无命中提示"未在项目内找到定义",预览与滚动位置不变
- [x] 6.5 多候选列表:展示文件路径 + 行号 + 种类 + 容器,按"同文件 > 同目录 > 其余"排序,**用户选择后才跳转**,不自动跳;候选排序中**定义优先于声明**,同一名字定义唯一而其余均为其声明时 SHALL 直接跳定义、不弹候选列表(排序为 定义 > 声明 > 同文件 > 同目录 > 其余;E2E 断言 `New` 弹出 2 条候选、**未自动跳转**、选择后才跳。另断言"定义优先":`compute` 在 util.h 有原型、widget.cpp 有唯一定义时直接跳定义且不弹列表)
- [x] 6.6 索引未完成且目标未索引时提示"符号索引仍在构建中,可稍后重试",不报错不空白(已实现三种提示:索引构建中 / 索引不可用 / 未找到定义。**验收边界**:"未找到定义"已由 E2E 断言;"索引仍在构建中"这一分支的触发依赖索引未完成的时间窗(旗舰项目 2.1 s 内即完成),写成断言会是不稳定的竞态,故未写 —— 如实标注为 E2E 未覆盖)
- [x] 6.7 新增 `src/intel/navStack.ts`:跳转前记录 `{path, line}`;**界面后退/前进按钮为主入口**(端点处置灰),**仅 macOS** 的 `⌥←`/`⌥→` 为补充键位绑定同一行为(Windows/Linux 该键位被浏览器后退/前进占用,不绑定不提示);栈端点处无操作不报错(界面后退/前进按钮为主入口,端点置灰;E2E 断言两级跳转后逐级回到原文件原行、一路后退到端点后按钮置灰且再点无操作、预览内容仍在)
- [x] 6.8 macOS 的 `⌥←` / `⌥→` 处理器**必须调用 `preventDefault`**(Windows/Linux 的 `Alt+←/→` 是浏览器后退/前进的默认绑定,故该平台不绑定、不提示);在 dev 测试钩子中记录 `defaultPrevented` 以供 E2E 断言;真实抢占行为由 12.x 真机清单核验(仅 macOS 绑定 ⌥←/⌥→ 并 `preventDefault`;dev 钩子 `window.__cv.lastNavKey` 记录 `defaultPrevented`,E2E 断言其为 true。**验收边界已在断言名与代码注释中写明**:CDP 合成按键只进渲染进程,证明不了浏览器有没有被拦住,那只能由 12.3 真机核验)(**已核验:验证人=用户本人 / macOS / 2026-08-21 / 结果:无问题、未被浏览器抢占**)
- [x] 6.9 所有导航入口(跳转 / 引用 / 大纲 / 符号搜索 / 全文搜索)统一走同一条"打开文件 + 定位行"通道并统一入栈(统一通道为 `navStack.navigateWithHistory`;跳转、多候选选择、引用结果、**大纲定位**、**符号搜索**全部经它入栈 —— 后两者原先只调 `gotoLine`/`navigateTo` 不入栈,与 spec「每次导航跳转之前 SHALL 记录当前位置」不符,已改)

## 7. 查找引用

- [x] 7.1 新增 `src/intel/references.ts`:三级漏斗扫描任务(语言组候选文件 → 文件文本 `indexOf` 预筛 → 命中文件 Lezer 解析)(三级漏斗:主线程按语言组筛候选文件 → Worker 内整份文本 `indexOf` 预筛 → 仅命中文件做 Lezer 解析)
- [x] 7.2 精筛排除注释与字符串:按命中位置查所在 Lezer 节点类型,落在 `Comment` / `String` 类节点内的出现丢弃(按命中位置 `resolveInner` 取最内层节点并向上查,命中 `Comment`/`String`/`CharLiteral` 类节点即丢弃;E2E 断言 service.go 中只出现在注释与字符串里的 `Serve` **不进入结果**)
- [x] 7.3 新增引用结果面板(预览区下方抽屉):按文件分组、行号 + 行内容片段 + 命中高亮,点击直达行
- [x] 7.4 流式回填(每批 ≤50 条),首批 1 秒内出现;面板明示"结果基于名称匹配,可能包含同名但无关的位置"(每批 ≤50 条流式回填;旗舰项目 8,500 文件上实测**首批 44ms**;面板常驻"结果基于名称匹配,可能包含同名但无关的位置"提示)
- [x] 7.5 命中数达上限(默认 1,000)或扫描预算耗尽时停止并明示已截断;触发入口为**预览区右键菜单**(原文的 `Shift+F12` 已被 2026-08-17 保守假定口径取代:F12 是浏览器保留键,不绑定也不提示)(上限 1,000 条 + 扫描文件数预算 20,000;旗舰项目上实测触发并显示"已达上限 1000 条,结果已截断";触发入口为右键菜单,`Shift+F12` 未绑定 —— F12 是浏览器保留键,按保守假定不绑不提示)

## 8. 全文内容搜索

- [x] 8.1 **结论:复用符号池的 `grep` 任务类型,不新建 `contentWorker.ts`**(独立 Worker 会整份复制 349 KB 的语法 chunk);实际落在 `src/search/contentStore.ts` + `ContentPanel.tsx`:跳过图片与二进制(`identifyByName` + `looksBinary` 前 8 KB),> 5 MB 只搜前 1 MB 并标记"部分搜索"(**采用任务原文允许的第二方案:复用符号池的 `grep` 任务类型**,未新建 `src/search/contentWorker.ts` —— 独立 Worker 文件会把带五套 Lezer 语法的 345 KB chunk 整份复制一遍。注:proposal.md「Impact」仍写着新增该文件,已列入 11.7 不一致清单)
- [x] 8.2 逐行字面量匹配,支持"区分大小写"开关(默认不区分),切换开关重新执行查询
- [x] 8.3 新增全文搜索面板与**不依赖快捷键的界面入口**(搜索面板"全文"模式);**仅 macOS** 绑定 `⌘⇧F` 作为补充键位并 `preventDefault`(Windows/Linux 未经真机核验前不绑定不提示,见 12.5);与文件名/符号搜索界面上可区分;仅项目模式提供(界面入口为搜索面板"全文"模式;**键位仅 macOS 绑定 `⌘⇧F`**,Win/Linux 依保守假定不绑定不提示 —— 任务原文的 `Cmd/Ctrl+Shift+F` 已被 2026-08-17 口径取代,已列入 11.7 清单)
- [x] 8.4 关键词 < 2 字符不启动全项目扫描并提示;展示扫描进度(已扫描文件数)与结束状态(完成 / 已取消 / 已截断),提供手动停止
- [x] 8.5 结果按文件分组、行号 + 行片段 + 命中位置标出,点击直达行并短暂高亮;流式回填,首批 1 秒内出现
- [x] 8.6 上限:总命中 2,000、单文件 50,达上限停止并明示截断(旗舰项目实测:搜 `local` 触发总命中 2,000 上限并显示"已达上限 2000 条,结果已截断";单文件上限 50 由 big.log 实测触发)
- [x] 8.7 取消与一致性:新查询 / 切换大小写 / 关面板 / 刷新目录树 / 切换项目立即取消在途搜索,过期结果不混排(靠全局 `generation`)(每次查询持有自己的 runId;池同时捕获全局代次,故切项目 / 刷新目录树连带作废。**与 design.md D4 字面的 "generation++" 不同**,已由方案会话确认 D4 初稿有误并更正)

## 8b. 单文件模式的代码理解范围

- [x] 8b.1 单文件模式下提供文件大纲(支持语言 + Markdown 标题),行为与项目模式一致
- [x] 8b.2 单文件模式下跳转限定当前文件内;目标不在本文件时提示"单文件模式下仅支持文件内跳转,打开所在文件夹可获得跨文件跳转"
- [x] 8b.3 单文件模式下不渲染查找引用与全局符号搜索入口,并在界面明示需要打开文件夹才能使用
- [x] 8b.4 确认单文件模式复用同一套抽取代码(不新增第二条代码路径),仅 UI 层按 `mode === 'single'` 决定入口与提示(单文件模式复用 `ensureFileSymbols` → `extractOne` → 同一个 `symbolWorker`,无第二条抽取路径;UI 层按 `mode === "single"` 决定入口与提示)
- [x] 8b.5 **修单文件模式跳转的两处接缝,并且按结构性改法修**(PM 2026-08-18 读代码发现第一处,方案会话复核发现第二处):现状 `jump.ts` 单文件分支直接取 `local[0]` 跳走 —— ① 文件内多条同名定义(Java / C++ 重载在同一文件极常见)被**静默跳到第一条**,违反"MUST NOT 静默跳到其中任意一个";② 该分支同时**绕过定义优先规则**,`.cpp` 顶部 `void foo();`、下方 `void foo() {…}` 时会跳到声明行。(**按结构性改法修**:不是在单文件分支里补一段候选逻辑,而是把该分支整条并入项目模式那条路径 —— 两种模式现在**只差候选来源**(`activeFileSymbols` vs `lookupDefinitions`)与"未找到"的文案,定义优先与多候选判定完全共用同一段代码,行为一致由结构保证而非靠两处各自维护。单文件来源另对齐了 `byLowerName` 的语义:按小写比较、排除 Markdown 标题)
  **改法不是"把项目模式那几行复制过来",而是让规则只有一份实现**:模式只决定**候选集合怎么来**(项目模式 = `lookupDefinitions` 查表,单文件模式 = `activeFileSymbols` 过滤),拿到集合后**两条路径走同一个决策函数**(定义优先取 `primary` → `primary.length === 1` 直跳 → 否则 `candidateList` + `rankCandidates`)。**验收标准:代码里 `if (mode === 'single')` 之后 MUST NOT 出现独立的跳转决策实现** —— 规则重复出现在两处实现里就是接缝的物理形态,结构上不给它落脚点,而不是靠人记得两边都改

## 9. 降级、刷新联动与边界

- [x] 9.1 符号总数达 **400,000** 上限时停止收录,状态置 `partial` 并提示"项目过大,符号索引仅部分完成"+ 已索引文件数,已索引部分继续可用(阈值取值理由见 design.md D5;提示文案中的数字与阈值保持一致)(阈值取值由旗舰场景断言"306,000 不得降级";降级**机制**用 dev 钩子把上限调低到 20 做确定性断言 —— 合成 400,000 符号来触发太贵,而取值与机制是两件事)
- [x] 9.2 单文件超 5 MB 或预览被截断时,在预览区与大纲区明示该文件符号未被索引 / 仅覆盖已加载部分(新增 6 MB 的 `src/huge.go` 用例;预览区与大纲区两处提示均已断言。**修掉一个真 bug**:命中索引缓存时会丢掉 `skipped` 原因,大纲把"文件过大未索引"错显成"未发现定义" —— 已在索引里持久化降级原因)
- [x] 9.3 Worker 异常终止时状态置 `unavailable` 并提示"符号索引不可用",目录树 / 预览 / Markdown / 主题 / `⌘K` 全部保持可用(触发用 dev 钩子模拟——真实 Worker 崩溃无法在 E2E 里稳定制造;验证的是降级 UI 契约:提示"符号索引不可用",且预览 / ⌘K / 主题切换均**实测仍生效**)
- [x] 9.4 单文件级增量:预览某文件时对比 `{size, lastModified}` 指纹,不一致就地重抽该文件符号,大纲/跳转/符号搜索反映最新内容
- [x] 9.5 `treeStore.refreshTree()` 联动:`generation++` → 符号索引全量重建、界面标识"索引重建中"、在途全文搜索取消;重建完成后新增文件符号可命中、已删除文件符号消失(实测:新增 `new-service.go` 后其符号可被命中、删除 `store.go` 后其符号从结果消失、重建期间顶栏显示"索引构建中")
- [x] 9.6 项目切换与回到首页时清空索引、导航栈、引用与搜索面板状态(断言前先把全文搜索面板真的打开,否则"切换后面板已关闭"是恒绿断言)

## 10. E2E 与回归门禁

- [x] 10.1 在 `scripts/e2e.mjs` 增加代码理解合成项目(OPFS):Go / TS / Java / Python / C++ 各若干文件,含跨文件唯一定义、两处同名定义、注释与字符串中的同名文本、形参与局部变量同名的函数、一个 `.yaml`、一个 `.md`、一个 50 MB 大文件
- [x] 10.2 断言大纲:条目内容正确、**不含形参与局部变量**、点击定位行号正确;`.yaml` 显示不支持说明;`.md` 显示标题大纲(含 Java/Go/C++/TS/Python 五语言的定义齐全 + 行号正确 + **形参与局部变量不出现**;`.yaml` 显示不支持说明;`.md` 显示标题大纲且跳过围栏内的 `#`;另加一条 MutationObserver 断言"切换文件过程中每一帧都不残留上一个文件的条目"——只看最终态是假绿)
- [x] 10.2b 断言 **Markdown 大纲可用而不只是存在**:富文本视图下点击大纲某标题 → 断言视口内出现**对应的渲染标题元素**(而非仅断言大纲条目存在)、断言仍停留在富文本视图;源码视图下点同一条目 → 断言定位到对应源码行。**根因备忘**:原 10.2 只断言了「`.md` 显示标题大纲」,证明了大纲存在、没证明它能用 —— Scenario 的后半句"并可点击定位"被吃掉就绿了(guide.md 加了 60 段填充把"安装"推到第 131 行、远在首屏之外 —— 否则整篇不滚动也在视口内,"滚动到可见区域"不写定位逻辑也恒真。断言:落点为 `H3[data-cv-line="131"]`、文本为"安装"、在视口内、`scrollTop` 由 0 变 1843(**确实发生了滚动**)、仍停留富文本视图未被切走;再切源码视图点同一条目断言定位到源码行)
- [x] 10.3 断言跳转:跨文件唯一定义落点行号正确;同名多候选**出现列表且未自动跳转**;无定义时提示且预览不变(跨文件唯一定义落点行正确 + 树选中同步;同名多候选出现列表且未自动跳转;无定义时提示且预览不变)
- [x] 10.3b 把「索引仍在构建中」分支(6.6)改造成**可确定断言**:在 dev 测试钩子中加一个暂停开关(`__CV_TEST_HOOK__` 保护、仅 dev 构建),Worker 池在任务派发边界检查它;E2E 流程 = 暂停索引 → 触发跳转 → 断言出现"索引仍在构建中"提示 → 恢复索引 → 断言跳转恢复正常。**理由**:该分支原先靠抢索引未完成的时间窗,旗舰项目 2.1 s 就索引完了,写成断言必然是竞态;与其留一条不稳定断言或一个空洞,不如给它一个确定的触发手段(同「12.5 用 UA 断言不提示键位」的思路)(用 `pool.ts` 的索引暂停开关做成确定性流程:暂停 → 断言"符号索引仍在构建中"提示且预览不变 → 恢复 → 断言同一跳转正常完成。无竞态)
- [x] 10.4 断言导航栈**可断言的部分**:两级跳转后连续两次"后退"(按钮触发)逐级回到原文件原行、端点处按钮置灰不报错、`Alt+←` 的 `keydown` 处理器 `defaultPrevented === true`。**断言旁注明:浏览器级后退/前进行为不可由 E2E 覆盖**(CDP 合成按键事件只进渲染进程、不触发浏览器 UI,原先"URL 未变"的断言无论有无 `preventDefault` 都会绿,属假绿,已删除)(按钮触发的两级后退、端点置灰、⌥← 的 `defaultPrevented === true` 均已断言;**断言名内已注明浏览器级前进后退不可由 E2E 覆盖**)
- [x] 10.5 断言查找引用:条数正确,注释与字符串中的同名文本被排除,点击直达行
- [x] 10.6 断言三入口互不干扰:同一关键词下文件名 / 符号 / 全文三种模式的结果集各自正确且互不影响;三者均**经界面入口(非快捷键)**触发,确保不依赖键位也可验收(三种模式经**界面入口**触发:文件名只返回文件名匹配、符号只返回符号匹配、全文返回正文命中——含仅在正文出现的 `config.yaml`,三者结果集互不覆盖)
- [x] 10.6b 断言单文件模式范围:单文件模式下大纲可用、文件内跳转可用、目标不在本文件时出现引导提示、引用与符号搜索入口不可用且有说明
- [x] 10.6c 断言单文件模式跳转与项目模式一致:合成一个含同名重载的文件(如 `foo(int)` / `foo(String)`)与一个"前置声明 + 定义"的 `.cpp` → 单文件模式下触发跳转 → 断言**出现候选列表且未自动跳转**、断言"声明+唯一定义"时落点在**定义**行;同一文件在项目模式下同一标识符的跳转结果 SHALL 与之一致(新增 `src/overload.cpp` 用例:2 条声明 + 2 条同名重载定义 + 1 组"声明 + 唯一定义"。项目模式与单文件模式**各跑一遍同一组探针**,断言 ①重载均弹出 4 条候选且未自动跳转 ②"声明+唯一定义"落点均在定义行(第 8 行)而非声明行(第 3 行) ③两种模式结果逐项一致。修复前这三条都会失败:旧实现会静默跳到第 1 行的声明)
- [x] 10.7 断言全文搜索:首批结果 < 1 s、点击直达行、二进制/图片被跳过、超大文件标记部分搜索、截断提示出现、手动停止生效(首批 < 1 s、点击直达行、二进制/图片被跳过(**带正向对照**)、超大文件标记部分搜索、总命中 2,000 截断、手动停止均已断言)
- [x] 10.8 断言规模与降级:10,000 文件项目下根目录首层仍 < 1 s、索引进度可见、索引期间可展开目录与预览;50 MB 文件出现"索引受限"提示(旗舰项目根层首屏 7ms;顶栏索引进度可见;索引期间目录树可交互;50MB 文件出现"符号索引受限"提示)
- [x] 10.8b 断言**旗舰场景不降级**:合成一个约 10,000 文件、支持语言占比 80% 以上的项目(仿 Java/Go 单体仓),索引完成后状态为"已完成"而非"部分完成",界面不出现"项目过大"提示(10,000 文件 / 支持语言占比 85% / 306,000 符号,终态为"索引已完成"而非"部分完成",无"项目过大"提示)
- [x] 10.9 断言跳转性能:索引完成后连续 20 次跳转,P95 < 200 ms(`performance.now()` 记录触发到目标行可见)(旗舰 10k 项目、索引完成后 20 次**跨文件**跳转:P95 **17–18ms**、中位 17ms、20/20 成功。计时在页内用 `performance.now()`,不含 CDP 往返)
- [x] 10.10 断言零网络:全流程(索引 + 跳转 + 引用 + 符号搜索 + 全文搜索)`httpRequests` 为空(全流程含索引 + 跳转 + 引用 + 符号搜索 + 全文搜索 + 旗舰 10k 项目,`httpRequests` 为空)
- [x] 10.11 **回归门禁**:既有 45 条断言全绿,新增断言与其合并输出到同一份报告(既有 45 条断言全绿;阶段一新增 41 条,合并输出为 86/86 PASS)

## 11. 验收与收尾

- [x] 11.1 实测记录:在 10,000 文件 OPFS 合成项目上记录索引真实耗时与 JS heap 峰值,写入 `docs/spike-code-intel-baseline.md`;**按判据核对:P0 语言占比 80% 的 10,000 文件项目不得进入 `partial` 状态**;与 design.md D5 预估偏差 > 2 倍、或判据不满足时**回报 PM 调整上限,不擅自改阈值**(实测记入 docs/spike-code-intel-baseline.md 第 3 节:306,000 符号、2.1–2.2 s、heap 39–44 MB,**判据满足**未进入 partial。⚠️ 耗时比预估快 2 倍以上,已按义务回报;并如实标注**测量在 OPFS 上,快于真实磁盘 FSA 句柄**,不能直接外推真机)
- [x] 11.2 体积校验:`npm run build` 后对比 1.3 的基线,确认主 chunk 未显著增长、总体积增幅在预估范围(约 +26%)且 < 50%;超出则回报(**最终产物**:主 chunk +2.8%、`symbolWorker` 349,598 B 独立 chunk、`dist/` 总计 1,344 KB 即 **+38.8%**;语法表全部落在独立 Worker chunk 不进首屏;**超出预估的 +26%**,未触 50% 硬阈值,已按"超出则回报"义务上报,详见基线文档第 1b 节)。**必须以最终产物重新量一遍**再写进 baseline —— design/proposal 里现存的是阶段二快照(主 chunk 962,965 B / 总量 +40%),每次构建都会变,别让 baseline 躺着一个中间态的数(PM 2026-08-17 校正:回报的 959,173 已被后续构建取代)
- [x] 11.3 零声明校验:确认 `public/manifest.json` 的 `permissions` 仍为空、无 `host_permissions`、无新增 CSP 指令;`grep` 产物确认无远程资源引用(`permissions: []`、无 host_permissions、无 CSP 指令;产物内除 SVG 命名空间字面量 `www.w3.org` 外无远程资源引用,E2E 全程 `httpRequests` 为空)
- [x] 11.4 用可判定指标评估 C/C++ 抽取质量(任一超阈值即回报 PM):① 误收率 =(被收成符号的形参+局部变量)/ 总符号数 ≤ 5%;② 多候选列表中位长度 ≤ 3;③ 定义漏收率(对人工标注样本)≤ 15%。样本 = E2E 合成 C/C++ 文件 + 一个真实开源 C/C++ 项目的抽样文件;数值写入 `docs/spike-code-intel-baseline.md`。若需降级为"仅大纲不跳转",**回报 PM 后再改 spec**,不擅自缩小范围。**另记 C/C++ 符号密度**(符号数/文件、符号数/KB)并与 Go 样本对比:阶段一的 306,000 符号 / 400,000 上限(余量 23%)测自 85% Go 的合成项目,而收录原型后 C/C++ 同一函数会在头文件与实现文件各出现一次、密度更高,那 23% 余量不可直接搬到 C/C++ 重仓;若密度达 Go 的 2 倍以上,回报方案会话与 PM 一并复看阈值(PM 2026-08-17 观察项,当前不动阈值——降级行为可见且优雅,不构成风险)(新增 `scripts/measure-cpp.mjs`;真实样本 = 本机 MacOSX.sdk 下的 LLVM libc++ 与系统 C 头文件,抽样 400 个文件 / 1.9 MB。①误收率合成样本 0.0%、②多候选中位长度 2、③漏收率 0.0%,均达标。**符号密度 0.13× Go,远未达 2 倍回报线**。抽查真实代码时发现并修掉一处误收:形参里的 `const struct utimbuf *` 被收成结构体定义 —— struct/class/union 现要求带定义体才算定义,消除 261 条误收)
- [x] 11.5 `npm run build`(含 `tsc --noEmit`)与完整 E2E 全绿;运行 `scripts/verify-icons.mjs` 确认打包完整性未回归(`npm run build` 含 `tsc --noEmit` 通过;完整 E2E **165/165**;`scripts/verify-icons.mjs` 8/8;`scripts/check-extract.mjs` 23/23)
- [x] 11.6 `openspec validate "add-code-intelligence" --strict` 通过(`openspec validate "add-code-intelligence" --strict` 通过)
- [x] 11.7 **全量过期指令核对**(收口必做,别靠留意靠一次扫描):把 tasks 里所有提到**具体键位、具体文件名/路径、具体阈值**的行逐条对照当前 spec/design 是否仍成立,不成立的**直接改原文**而不是靠勾选行备注兜。已发生两次(5.3 的 Win 备用键位、7.5 的 `Shift+F12`),根因是工件没跟着裁定同步更新 —— 属流程漏洞,不是开发疏忽(扫描已执行:按"具体键位 / 文件名路径 / 阈值"三类逐行核对 tasks + proposal + 4 份 spec。4 份 spec 无过期项;查出 8 处不一致——proposal 4 处(符号搜索"改绑备用键位"、全文搜索 `Cmd/Ctrl+Shift+F`、导航栈未注明仅 macOS、Impact 里写了实际不存在的 `contentWorker.ts`)、tasks 4 处(8.3 键位、8.1 文件名、6.7/6.8 主语、12.5 的 `navigator.platform`)。清单已交方案会话,**8 条均已改在原文**而非靠备注兜)
- [x] 11.8 按**固定断言自查口径**(design.md D8,PM 2026-08-17 定)逐条复核全部 E2E 断言与实测指标,两问:①**功能没实现的话它会不会照样成立?**(假绿)②**它测的是不是我要保证的那条路径?**(假数字)。答不上任一问的断言必须重写或删除;本 change 已出现三次同类事件(`Alt+←` URL 断言、大纲只断言最终态、P95 样本符号名重复致跳转退化),复核结果记入 baseline(逐条过两问,查出并修掉 4 条:图片/二进制跳过缺正向对照、主题可用性只查元素存在、切项目清空面板恒绿、索引期间可交互只数行数。详见 baseline 第 5b 节)
- [x] 11.9 **正交矩阵兜底走查**(本 change 试用 PM 2026-08-18 的第三条方法):挑"会改变代码路径"的维度做叉乘,逐格确认有断言或有明确的 n/a 理由,填好的矩阵写进 baseline。至少覆盖 ①模式(项目 / 单文件)× ②候选情形(唯一定义 / 多定义 / 定义+声明 / 无定义 / 索引未就绪);建议再叠 ③语言支持档(full / outline-only / none)× ④文件是否被截断。这次的两处偏差在该矩阵里是**相邻的两格**,一眼可见 —— 说明矩阵能捞出前两道结构性防御漏掉的组合。(矩阵见 baseline 第 5c 节,三张:模式×候选情形、语言支持档×能力、文件规模/索引状态×代码理解。**捞到 3 个格子**并已全部补上断言:①单文件模式无"符号未就绪"态,导致符号就在本文件里却提示"打开所在文件夹"——是错误建议而非少个提示;②Markdown 标题不进符号搜索无断言;③不支持语言"跳转入口确实不存在"无断言。三者共同形状:都不在"功能能不能用"线上,而在"**不该有的东西确实没有**"线上——断言天然偏向证明存在。**保留 5 个公开空格**并写明不补理由(重复覆盖同一分支/共用同一门控))
  **防自欺条件(PM 2026-08-18 追加)**:矩阵最容易变成它自己要防的东西——逐格写"已覆盖"然后归档,而**功能坏掉的话那张表照样是满的**(第①问的完美案例)。因此每一格 MUST 是下面三种之一,不许有第四种:① **具体断言编号**(如 `10.6c-2`),写"已覆盖"不算;② **明确的 n/a 理由**(如"单文件模式无索引未就绪态,因为只索引一个文件");③ **公开的空格** —— 照实留空并说明这次为何不补(成本 / 优先级 / 留给 change #2)。**第三种存在,这张表才是诚实的;一张填满的矩阵反而可疑**,真去逐格想不可能一个空都没有。baseline 里除矩阵本身外还 MUST 写明"它捞到了什么、哪里累" —— 试用的结论不是"好用",而是**具体捞到了几个格子**;捞到 0 个也是有效结论,说明前两条结构性办法已把活干完,第 3 条以后不必固化

## 12. 人工真机验证清单(E2E 覆盖不到,必须真机按键)

> 浏览器级快捷键冲突无法自动化验收:CDP 合成按键事件只进渲染进程,不触发浏览器 UI。以下每项勾选时**必须注明验证人、操作系统、Chrome 版本、日期**;未注明视为未验证。
>
> **Windows/Linux 无真机时的处置口径(PM 2026-08-17 裁定)**:当前用户环境为 macOS,大概率拿不到 Windows 真机。Windows/Linux 相关项**不得勾选**,一律标注 `未验证(无 Windows 真机)`,并按**保守假定**实现:假定 `Ctrl+Shift+O` 与 `Alt+←/→` 在 Win/Linux **均被浏览器抢占**。即 Win/Linux 平台**不承诺任何 `Ctrl+Shift+*` 键位**、UI 上**不显示未经核验的键位提示**(不能让用户看到一个按了没反应的提示),能力全部由界面入口保证。宁可承诺少一点,也不要承诺一个没验证过的键位。一个被随手勾上的清单比没有清单更糟,因为它伪造"已验证"的假象。

- [x] 12.1 macOS Chrome 真机:`⌘⇧O` 是否被浏览器抢占(预期不抢,Mac 书签管理器为 `⌥⌘B`)—— 记录实际行为(**已核验:验证人=用户本人 / macOS / 2026-08-21 / 结果:无问题、未被浏览器抢占**)
- [x] 12.2 macOS Chrome 真机:`⌘⇧F` 的真实行为(是否被浏览器占用、页内查找是否被误触发)(**已核验:验证人=用户本人 / macOS / 2026-08-21 / 结果:无问题、未被浏览器抢占**)
- [x] 12.3 macOS Chrome 真机:`⌥←` / `⌥→`(macOS 的 `Alt+←/→`)是否被浏览器或系统抢占,`preventDefault` 是否生效(**已核验:验证人=用户本人 / macOS / 2026-08-21 / 结果:无问题、未被浏览器抢占**)
- [x] 12.4 macOS Chrome 真机:`F12` 的实际行为;确认主入口(`⌘`+点击、右键菜单)不受影响(**已核验:验证人=用户本人 / macOS / 2026-08-21 / 结果:无问题、未被浏览器抢占**)
- [x] 12.5 按保守假定实现 Win/Linux 平台行为:不绑定也不提示任何 `Ctrl+Shift+*` 键位、不提示 `Alt+←/→`,按 **UA** 判定平台后(不用已废弃的 `navigator.platform`,且 UA 可在 E2E 中改写故可断言)仅呈现界面入口;E2E 断言"非 macOS UA 下 UI 不出现未核验键位提示"(`src/lib/platform.ts` 按 UA 判定;E2E 以 Windows UA 断言全页文本与 placeholder/title 中均不出现 `⌘⇧O`/`⌘⇧F`/`Ctrl+Shift+*`/`Alt+←→`/`F12`,且界面入口仍完整)
- [x] 12.6 在 README「已知限制」与 `docs/spike-code-intel-baseline.md` 中如实写明:Windows/Linux 键位未经真机核验,该平台仅通过界面入口提供全部能力(README「已知限制」新增 Windows/Linux 键位未经真机核验的说明;基线文档第 5 节同步记录)
- [x] 12.7 将 12.1–12.6 的结论汇总写入 `docs/spike-code-intel-baseline.md`,并同步 design.md D7 冲突表中"需真机核验"的行;Windows 真机验证作为遗留项移交 change #2,拿到设备后回填(baseline 第 5 节重写为四小节:5.1 macOS 已核验四组键位(验证人=用户本人 / 2026-08-21 / 均未被抢占)、5.2 Win/Linux 未验证及原因、5.3 自动化覆盖的部分、5.4 真实磁盘实测未验证。design.md D7 冲突表加了"真机核验状态"一列并逐行填入,同时把已被取代的"Win/Linux 改绑备用键位"更正为"不绑定也不提示")
- [ ] 12.8 (人工,不设门禁)真实磁盘 10k 项目索引耗时实测:阶段一的 2.1 s 测自 **OPFS**,而 D5 的 I/O 估算针对真实磁盘 FSA 句柄,该数值**不可外推到用户真机**。需用户手势选一个真实的万级文件目录后记录耗时与 heap,写入基线文档第 3 节;拿不到样本时标注 `未验证(无真实万级样本)`,**不阻塞交付** —— 对用户的承诺是"首屏 < 1 s、进度可见、索引期间不阻塞"这三条可断言行为,而非某个绝对秒数**未验证(无真实万级样本)** —— 按既定口径不设门禁,也不为了勾选而凑一个假样本。阶段一的 2.1 s 测自 OPFS,不可外推真机;对用户的承诺是"首屏 < 1 s、进度可见、索引期间不阻塞"这三条可断言行为,均已由 E2E 覆盖。已记入 baseline 第 5.4 节

## 13. 阶段交付与汇报

- [x] 13.1 阶段一(索引 + 大纲 + 全局符号搜索)完成:E2E 相关断言全绿,向方案会话回报,由方案会话汇总给 PM 转达用户(阶段一 E2E 86/86 全绿;已回报方案会话)
- [x] 13.2 阶段二(跳转 + 多候选 + 导航栈 + 查找引用)完成:同上回报(阶段二 E2E 125/125 全绿;已回报方案会话)
- [x] 13.3 阶段三(全文内容搜索)完成:同上回报;三阶段全部完成后由 PM 统一确认收口(阶段三 E2E 165/165 全绿;已回报方案会话)
