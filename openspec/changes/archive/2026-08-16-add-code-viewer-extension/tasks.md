## 1. 项目脚手架与 Spike

- [x] 1.1 初始化项目:TypeScript + Vite + Preact,MV3 manifest + 最小 service worker(`chrome.action.onClicked` → 打开整页查看器),开发者模式可加载
- [x] 1.2 Spike:扩展页内 `showDirectoryPicker()` → 句柄存 IndexedDB → 重启浏览器后 `queryPermission`/`requestPermission` 重连,验证授权弹窗频率并记录结论(2026-08-16 用户真机实测通过,授权体验可接受、D1 成立;结论详见 docs/spike-1.2-handle-persistence.md)
- [x] 1.3 搭建查看器页面骨架:左树右预览的双栏布局 + 顶部栏,VS Code Dark+ 风格基础样式

## 2. 项目访问层(project-access)

- [x] 2.1 实现"打开文件夹"/"打开文件"入口(含用户取消的无害处理)
- [x] 2.2 实现句柄持久化与最近项目列表(最多 10 个、按时间排序、可移除)
- [x] 2.3 实现最近项目重连流程:权限查询、用户手势内续期、句柄失效的降级提示

## 3. 目录树(file-tree)

- [x] 3.1 数据层:惰性读取目录层级,展开状态维护,目录优先 + 名称自然排序
- [x] 3.2 UI 层:扁平化列表 + 固定行高虚拟滚动,目录/文件图标,展开/折叠交互
- [x] 3.3 文件选中联动:点击文件触发预览加载,选中态高亮,读取失败的错误提示
- [x] 3.4 手动刷新:重读目录、按路径恢复展开/选中态;点击文件即重读内容(不缓存),覆盖外部修改场景
- [x] 3.5 千级条目目录展开的分批入列渲染(不等 `entries()` 全部迭代完)
- [x] 3.6 用 1,000+ 条目目录与约 10,000 文件的项目做性能验证(首屏 <1s、滚动流畅)

## 4. 文件识别与渲染分发(file-preview)

- [x] 4.1 实现扩展名 → 类型/语言映射表,及无扩展名时的内容嗅探(NUL/不可打印字符比例、shebang)
- [x] 4.2 实现渲染分发器:代码 / Markdown / 图片 / 二进制提示 / 纯文本 五个通道
- [x] 4.3 实现大文件降级:size 门限(5 MB)+ 截断读取(前 1 MB)+ 截断提示,不阻塞 UI
- [x] 4.4 统一文本解码:TextDecoder(utf-8, 非 fatal),截断边界丢弃末尾不完整字节序列;README 已知限制注明仅支持 UTF-8

## 5. 代码高亮渲染

- [x] 5.1 集成 CodeMirror 6 只读模式:行号、只读保证、选择/复制可用
- [x] 5.2 接入目标语言包:Python/Java/C/C++/Go/JS/TS/JSON/HTML/CSS/SQL/YAML/XML/Markdown 源码(官方 Lezer 包)+ Shell(@codemirror/legacy-modes)
- [x] 5.3 自建接近 VS Code Dark+ 的 CM6 高亮主题

## 6. Markdown 与图片

- [x] 6.1 集成 markdown-it(GFM)+ DOMPurify 净化,富文本样式与整体主题一致
- [x] 6.2 Markdown 代码块用 @lezer/highlight 静态高亮
- [x] 6.3 实现"渲染视图 ⇄ 源码视图"切换
- [x] 6.4 Markdown 相对资源处理:相对图片经目录句柄解析转 objectURL(失败/单文件模式给占位符),相对链接项目内导航(失败置灰),远程图片替换为占位符
- [x] 6.5 图片预览通道(PNG/JPEG/GIF/SVG/WebP,`<img>` + objectURL,自适应缩放)

## 7. 验收与收尾

- [x] 7.1 按三个 spec 的场景逐条走查验收(含恶意 Markdown 净化、50 MB 大文件、二进制文件)(备注:E2E 经 OPFS 句柄进入项目,授权路径相关场景——原生 picker/最近项目重连/句柄失效降级——待真机人工验收,依赖 1.2 spike 用户实测;已由用户真机实测覆盖(2026-08-16))
- [x] 7.2 确认零网络请求(DevTools Network 面板核查)
- [x] 7.3 补 README:安装(开发者模式加载)、使用说明、已知限制(受保护目录等)

## 8. 用户实测反馈改进(2026-08-16)

- [x] 8.1 浅色/暗色双主题:默认白底浅色(参照 VS Code Light),右上角切换控件,localStorage 持久化;CSS 变量 + CM6 主题扩展成对实现,覆盖整体 UI、代码高亮与 Markdown 样式(初版 Dark+ 转为暗色档)
- [x] 8.2 文件名快速搜索:Web Worker 后台建索引(不阻塞首屏)+ 顶部搜索框 + Cmd/Ctrl+K 聚焦 + 结果选中打开预览并同步树选中态 + 索引未就绪提示 + 手动刷新同步重建索引(刷新后不搜到已删文件、不漏新文件)
- [x] 8.3 Markdown 渲染视图排版:正文基准 16px、行高 ≥1.6,标题/代码块按比例调整
- [x] 8.4 侧栏宽度可拖拽调整(约 180–600px 限制,localStorage 持久化)
- [x] 8.5 E2E 补充上述各项的用例并重跑全绿(含浅色默认与切暗色后重开保持的主题用例;Ctrl+K 拦截断言——焦点落在搜索框而非浏览器地址栏;刷新后索引一致性用例;浅色主题下原有用例回归)
