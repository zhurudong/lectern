## 1. Baseline and dependency gates

- [x] 1.1 Record the current `npm run build`, `npm run check`, main chunk min+gzip, total `dist` size, and release zip size before Git code is imported; confirm existing unrelated worktree edits remain untouched. (2026-09-02 baseline: build/check green; `viewer` 1,060,289 B min / 380,302 B gzip-9; `dist` files 1,442,165 B, `du` 1,440 KiB; web-store-layout zip 520,028 B. Existing `chase-dockerfile-highlight-flake` edits remained unchanged.)
- [x] 1.2 Add `@codemirror/merge@^6.12.2` and `ignore@^7.0.8` to `package.json`/lockfile, verify the lock changes only for those dependencies, and record licenses. (Installed 6.12.2 / 7.0.8, both MIT. No existing `node_modules/*` version changed; npm only synchronized stale root lock metadata for the already-declared Lezer direct dependencies and package license. Production audit: 0 vulnerabilities.)
- [x] 1.3 Add a minimal lazy Git worker import, rebuild, and record main chunk, Git worker chunk, total min+gzip and zip deltas; the main entry MUST NOT eagerly contain the Git object reader. (Worker skeleton is a separate 133 B min / 149 B gzip-9 chunk and starts only for `projectView=changes`; main delta +345 B min / +179 B gzip-9, total files +478 B, release zip +466 B. No Git object code is in the main entry.)
- [x] 1.4 Run the shipped-artifact invariant checker after the dependency/import spike; if any network, write, permission or CSP invariant fails, stop and revise the dependency path rather than weakening the checker. (`npm run check` green on the freshly built artifact: zero network symbols, zero filesystem-write symbols, zero permissions/CSP changes; checker unchanged.)
- [x] 1.5 Add a deterministic Git-oracle test helper that creates repositories under a temporary directory with isolated user/system config and can copy the resulting worktree plus `.git` into the existing OPFS fixture path. (`scripts/git-fixture.mjs` fixes config/identity/time, snapshots binary `.git` bytes, and installs them into OPFS; repeated `check:git-fixture` runs produced identical commit OIDs.)

## 2. Read-only repository access

- [x] 2.1 Define structured `RepoUnavailableReason` and `GitReadError` types for non-repo, external gitdir, unsupported object format, missing/corrupt object, incomplete history, resource limit and repository-changing states.
- [x] 2.2 Implement a read-only FSA path layer with normalized relative paths, directory iteration, file metadata and bounded `File.slice`; expose no write operation and reject traversal outside the authorized root.
- [x] 2.3 Implement repository probing for a root `.git` directory and an in-root relative `gitdir:` file; distinguish absent metadata, malformed pointer and outside-authorized-root pointer.
- [x] 2.4 Add tests for standard repo, non-Git directory, malformed `.git` file, in-root gitdir and absolute/escaping gitdir; assert external absolute paths are not leaked in UI-safe errors. (`check:git-access` covers POSIX/Windows absolute paths, traversal, bounded slices, no write surface and public-error redaction.)

## 3. Object identity and loose objects

- [x] 3.1 Implement incremental SHA-1 for Git object identity, verify standard SHA-1 vectors and Git `blob <size>\0<body>` vectors against `git hash-object`; document that it is not used for security. (`Sha1` supports chunked updates; empty/abc/million-a and Git blob-ID oracle vectors pass. The implementation comment explicitly excludes security use.)
- [x] 3.2 Implement loose-object lookup and `DecompressionStream('deflate')` decoding for commit/tree/blob/tag, including header type, declared size and requested-OID verification. (`readLooseObject` validates the full loose-object envelope and returns copied bodies with typed source metadata.)
- [x] 3.3 Enforce the 16 MB metadata-object limit and typed failures for invalid headers, size mismatch, unsupported type, decompression failure and OID mismatch. (`check:git-loose` covers resource, type, size, identity and truncated-deflate failures; blobs are independently capped at 5 MB for the later UI contract.)
- [x] 3.4 Generate loose commit/tree/blob/tag fixtures and compare every decoded result with `git cat-file --batch`; include corrupt and truncated negative fixtures. (The deterministic fixture covers all four types through the binary batch protocol and targeted malformed replacements.)

## 4. Pack index, packfile and delta reader

- [x] 4.1 Parse pack index v2 fan-out, OID, CRC, 32-bit offset and 64-bit large-offset tables with bounds checks and binary OID lookup. (`PackIndexV2` verifies table ordering/bounds and the index checksum; a synthetic >4 GiB offset fixture passes.)
- [x] 4.2 Build a compact per-pack offset order that maps an object start to the next entry boundary without loading the `.pack` file; cover duplicate/invalid offsets and pack/index mismatch. (BigUint64/Uint32 ordered tables drive binary boundary lookup; duplicate, pre-header and count/checksum mismatches fail explicitly.)
- [x] 4.3 Parse pack v2 headers and variable-length object headers from bounded slices, isolate the exact zlib payload using adjacent offsets, and decode non-delta objects. (`PackFileV2` reads only header, trailer and one indexed entry span, validating entry CRC and declared inflate size.)
- [x] 4.4 Implement the delta instruction interpreter with source/result-size validation, copy/insert bounds checks, zero-op rejection and deterministic typed errors. (`applyGitDelta` passes randomized copy/insert reconstruction plus targeted malformed bytecode cases.)
- [x] 4.5 Implement OFS_DELTA base-offset decoding and REF_DELTA cross-pack/loose base lookup, with cycle detection and a maximum chain depth of 64. (Real Git OFS/REF packs and synthetic cross-pack, loose-base, self-cycle and 65-deep chains pass.)
- [x] 4.6 Add the 32 MB decoded-object LRU, per-object resource limits and reconstructed-object OID verification; oversized blobs may expose header metadata but MUST NOT yield partial content. (A 34 MB decoded fixture stays at 32 MB; oversized packed blobs return type/OID/declared-size context only.)
- [x] 4.7 Generate normal pack, forced OFS_DELTA and forced REF_DELTA fixtures, and compare all decoded objects against `git cat-file`; include multiple packs and large-offset index coverage. (`check:git-pack` compares 137 real packed objects through `cat-file --batch`, including forced delta modes and a two-pack repository.)
- [x] 4.8 Add randomized valid delta round trips plus malformed header, truncated zlib, out-of-range copy, delta cycle, depth overflow and corrupt reconstructed-OID tests; prove each negative path fails for the intended reason. (100 deterministic randomized rounds and every named negative path assert both error code and diagnostic stage.)
- [x] 4.9 Instrument FSA reads with a large pack fixture and assert no code path requests the whole `.pack` buffer; fail the test if a pack `arrayBuffer()` or an unbounded slice is attempted. (34+ MB pack fixture recorded 243 bounded reads and zero `readAll`/whole-file slices.)

## 5. Local refs, tags and Commit SHA

- [x] 5.1 Parse `HEAD`, loose refs, symbolic ref chains and `packed-refs`, including cycle detection and deterministic loose-over-packed precedence. (`readGitRefs` merges packed then loose refs and resolves symbolic chains through one cycle-checked path.)
- [x] 5.2 Detect SHA-256 repositories and authorization-external alternates as unsupported, with specific reasons; do not truncate OIDs or fall through to “missing object”. (Both config and observed 64-hex widths map to `unsupported-object-format`; escaping/absolute alternates map to `external-alternates`.)
- [x] 5.3 List and group `refs/heads/*`, `refs/remotes/*` and `refs/tags/*`, compute short SHA labels, and derive the initial base using remote HEAD name → `main` → `master` → current HEAD. (All fallback levels are tested; every remote ref carries explicit `localSnapshot: true`.)
- [x] 5.4 Peel lightweight/annotated tags recursively to commits and resolve full or uniquely abbreviated SHA across loose objects and pack indexes; reject ambiguous, missing and non-commit inputs without changing the selected endpoint. (`peelToCommit` and `resolveCommitInput` return only validated commit OIDs and typed failures.)
- [x] 5.5 Add oracle tests for loose/packed refs, detached HEAD, nested annotated tags, local-vs-remote grouping, remote “local snapshot” data and ambiguous SHA prefixes. (`check:git-refs` covers all named shapes using a system-Git fixture plus hostile synthetic refs.)

## 6. Commit graph and immutable snapshot comparison

- [x] 6.1 Parse commit parents/tree and tree entries (regular, executable, symlink and gitlink modes) with raw-byte bounds checks and stable display-path conversion. (`parseCommit`/`parseTree` validate object widths and modes; invalid UTF-8 is deterministically `\\xNN` escaped.)
- [x] 6.2 Implement exact best-common-ancestor calculation with parent caching; distinguish one merge base, no common history, incomplete/shallow history and multiple best bases. (`CommitGraph` computes the undominated common-ancestor set and returns a discriminated result for every case.)
- [x] 6.3 Implement recursive tree comparison that skips equal tree OIDs, merges paths deterministically and emits A/M/D plus mode/type/gitlink metadata; do not infer renames. (`compareTrees` walks changed subtrees only, expands file/directory replacement into factual D/A paths and retains raw paths.)
- [x] 6.4 Compare merge-base and tree-diff results against `git merge-base` and `git diff --raw --no-renames` for linear, diverged, unrelated, merge and criss-cross fixtures. (`check:git-graph` matches both Git oracles across all five graphs, including two criss-cross best bases.)

## 7. Current worktree snapshot

- [x] 7.1 Implement root and nested `.gitignore` plus accessible `.git/info/exclude` evaluation with `ignore`, hard-exclude `.git`, and document/test that authorization-external global excludes are not read. (Layered directory-scoped matchers honor negation; ignored parents are pruned except for known tracked descendants.)
- [x] 7.2 Traverse regular worktree files in the worker and incrementally compute Git blob OIDs from chunked file reads; do not use size/mtime alone as the equality decision. (`hashWorktreeFile` hashes the Git header plus immutable `File.slice` chunks capped at 1 MiB.)
- [x] 7.3 Compare a commit tree with the worktree snapshot, including tracked modification/deletion and non-ignored untracked files, while keeping index/staged state out of the endpoint model. (`compareTreeToWorktree` uses only HEAD tree facts and disk bytes; it never reads the index.)
- [x] 7.4 Add explicit metadata for the FSA mode-bit/symlink limitation and ensure a worktree “no content changes” state does not claim complete permission equivalence. (Every worktree snapshot is `confidence: limited` with mode/symlink and global-exclude limitation codes, including empty manifests.)
- [x] 7.5 Oracle-test nested ignores, negation, tracked ignored files, untracked files, same-size content changes, large files, deletion and staged-vs-worktree divergence against an isolated Git repository. (`check:git-worktree` covers every named case, mode-only degradation, 6 MiB chunking and Git blob-ID parity.)

## 8. Comparison service and worker lifecycle

- [x] 8.1 Define serializable endpoint, ref-group, compare-request, changed-file, file-pair and confidence types; keep repo-unavailable, exact empty, limited and error states distinct. (`protocol.ts` uses discriminated unions throughout; `ready/files=[]` is the only exact-empty representation.)
- [x] 8.2 Implement `review` as merge-base snapshot → target snapshot and `direct` as selected base → selected target; worktree contributes HEAD to graph ancestry and disk content to the target snapshot. (`ComparisonSession` freezes endpoint OIDs, then routes both modes through the same tree/worktree comparers.)
- [x] 8.3 Implement lazy worker messages for probe/list-refs, compare, load-file-pair and dispose; transfer only manifests initially and load blob pairs for the selected file on demand. (The built lazy worker is 45.68 kB minified; manifests contain no file bodies.)
- [x] 8.4 Add generation handling that terminates/recreates an obsolete worker when endpoints change, so an expensive stale request cannot block or overwrite the newest result. (`GitWorkerClient` aborts pending promises, terminates the prior worker and ignores stale-generation responses.)
- [x] 8.5 Add integration tests for default endpoint selection, both modes, current-worktree target, empty exact result, no/multiple merge base, object failure isolation and repository mutation during read. (`check:git-service` covers every named state plus a deterministic slow/stale worker race.)

## 9. Project view and branch-selection UI

- [x] 9.1 Add the project-only `files | changes` view state; switching views must preserve selected file, caret, navigation stack, search/index state and project context. (`projectView` is an orthogonal signal reset only on mode/root transitions; switching it does not mutate any reader/search/navigation state.)
- [x] 9.2 **Overall-visual gate:** produce at least three complete Git comparison page directions covering normal diff, the selected ref-picker-open interaction, and empty/error states; obtain the user's explicit selection and record the chosen reference before starting tasks 9.3–11.4. (User selected displayed direction 2 on 2026-09-02; the anchored ref interaction remains visual #2.)
- [x] 9.3 Implement the “文件 / 变更” entry and M/A/D grouped change navigation in the user-selected overall layout, with counts, stable path ordering, first-result selection and explicit loading/empty/unavailable/error states. (`GitComparison` renders the selected three-track page and every discriminated service state; browser fixture shows stable 3 M / 2 A / 2 D ordering.)
- [x] 9.4 Build the selected dual-reference control group around `基准 [ref] ⇄ 目标 [ref] [审查改动/直接比较]`, including short SHAs, default worktree target and a disabled-with-reason swap when worktree is selected; its placement follows the separately selected overall visual. (Worktree shows `HEAD <short SHA>` and disables swap with its target-only reason; immutable tag/branch endpoints swap in direct mode.)
- [x] 9.5 Build the selector-anchored ref panel from branch-interaction visual #2: fixed search, grouped current workspace/local/remote/Tag sections, “本地快照” text badge and bottom Commit SHA entry; do not add a centered modal or drawer. (Final light/dark evidence was reviewed in the implementation conversation.)
- [x] 9.6 Implement ref filtering and the inline Commit SHA resolution state; invalid/ambiguous/non-commit input must preserve the old endpoint and current diff. (Worker protocol gained `resolve-commit`; browser invalid-input proof retained `当前工作区` and all seven files.)
- [x] 9.7 Implement full keyboard operation and focus restoration for the ref panel, comparison mode, swap control and change navigation; Escape returns to the opening selector. (Native controls plus arrow/Escape picker handling; browser accessibility snapshot showed the opening target selector active after Escape.)
- [x] 9.8 Do not implement the exploratory mockup’s AI action or AI panel in this change, and do not ship dead AI placeholders; use the user-selected Git-only visual for the remaining layout. (`check:git-ui-contracts` fails if AI action/placeholder tokens enter the Git UI source.)

## 10. Unified diff and honest degradation

- [x] 10.1 Implement lazy selected-file loading for added, modified and deleted files, choosing target filename language (or base for deletions) without mutating normal preview state. (Manifest-first worker flow loads only the selected pair; `projectView` remains orthogonal to normal preview signals.)
- [x] 10.2 Render ordinary text with `@codemirror/merge` unified view, correct old/new line numbers, diff markers, current/total hunk count and initial positioning at the first hunk. (`UnifiedDiff` positions at `chunks[0].fromB`; browser text fixture renders deletion/insertion lines and `1 / 1`.)
- [x] 10.3 Configure CodeMirror read-only and remove accept/reject/revert/merge controls; add a DOM-level negative assertion for their absence and input/paste/drag/IME immutability tests. (`EditorState.readOnly`; browser input/paste proof retained identical DOM text and found zero merge-control nodes; the contract mutation test fails on `mergeControls:true`.)
- [x] 10.4 Route binary, gitlink, type/mode-only and either-side-over-5-MB files to metadata views with sizes/OIDs/reasons; MUST NOT render partial or garbled text diff. (`metadataOnly` normalizes observable worktree regular files, then routes binary/type/mode/resource states with both side facts.)
- [x] 10.5 Replace the default `scanLimit: 500` behavior with a bounded timeout and surface `precise === false` as a persistent “近似结果，可能遗漏细节” warning. (`timeout:900`; a >32 KB sparse fixture yields 1 merged chunk at scanLimit 500 versus the correct 12 with the bounded timeout.)
- [x] 10.6 Wire previous/next buttons and keyboard shortcuts to the same CodeMirror chunk-navigation commands, update the hunk counter, and register the shortcuts in keyboard help. (`Alt-ArrowUp/Down` live in the single `KEYS` registry and share `goToPreviousChunk`/`goToNextChunk` with the buttons.)
- [x] 10.7 Ensure returning to “文件”, changing files, closing a picker and hitting hunk boundaries all leave focus on the expected interactive target, never `body`. (View switch is a native button; selected-file controls retain focus and diff navigation focuses the editor; Escape explicitly restores the opening selector.)

## 11. Styling and accessibility

- [x] 11.1 Reproduce the overall visual selected in task 9.2 using existing Lectern tokens, while matching branch-interaction visual #2 specifically for the dual selector and anchored picker; capture light-theme screenshots against both references. (`design-qa.md` records two combined source/implementation passes and final `passed`.)
- [x] 11.2 Tune a complete dark-theme treatment for additions, deletions, selected rows, borders, badges, approximate/error notices and focus rings; verify color is never the only status signal. (Dark screenshots cover diff and picker; every file state retains M/A/D text.)
- [x] 11.3 Add semantic names, current values, expanded/selected/disabled states and live loading/error announcements for selectors, mode, ref groups, changed files and hunk navigation. (Browser DOM snapshot confirms dialog/listbox/options, named combobox, expanded/selected/current states and live compare text.)
- [x] 11.4 Keyboard-only and screen-reader smoke-test the complete flow from “变更” → choose refs/SHA → switch mode → select file → navigate hunks → return to “文件”. (In-app-browser semantic flow covered the full control order, invalid SHA, direct mode, file selection, hunk controls and focus restoration.)

## 12. End-to-end truth tests

- [x] 12.1 Add E2E coverage for all five local source types, local-snapshot labeling, default review semantics, direct compare, endpoint swap and worktree-only-as-target behavior. (The development-only real loose-object OPFS fixture exposes worktree/local/remote/tag/SHA; browser flow verified local-snapshot labels, both modes, immutable swap and the worktree guard.)
- [x] 12.2 Add E2E coverage for non-repo, external gitdir, SHA-256, invalid/ambiguous SHA, missing/corrupt object, no/multiple merge base, binary and >5 MB states; assert each shows its reason rather than an empty diff. (Development-only real-object fixtures drove the in-app-browser matrix. Every state showed its specific unavailable/error/metadata reason and none showed “没有变化”; invalid and generated ambiguous 4-hex SHA inputs retained the previous endpoint and seven-file result. No-common-history and multiple-best-base states now have distinct recovery text.)
- [x] 12.3 Add E2E coverage for worktree tracked/untracked/ignored/deleted changes and the permission-bit caveat; verify `.git` never appears as a changed path. (The worktree fixture rendered exactly `M tracked.txt`, `A untracked.txt`, and `D deleted.txt`; `ignored.log` and `.git` were absent, while the permission/symlink and external-global-excludes limitations remained visible.)
- [x] 12.4 Add a deterministic slow-worker hook and prove that rapid endpoint changes display the newest generation only and do not wait behind the obsolete computation. (A development-gated 2,000 ms initial delay was superseded by selecting `v0.3.1`; the newest generation applied in 651 ms, remained selected after the obsolete delay elapsed, and the stale seven-file result never overwrote the exact-empty result.)
- [x] 12.5 Make the no-merge-controls assertion fail once by temporarily enabling a control, then restore the implementation and record the negative-control result. (`check:git-ui-contracts` mutates an in-memory source copy to `mergeControls:true`, proves the guard fails, and then validates unchanged production source.)
- [x] 12.6 Make the diff-fidelity assertion fail once with CodeMirror’s default `scanLimit: 500` on a >32 KB sparse-change fixture, then restore timeout/precision handling and record the negative-control result. (`check:git-ui-contracts`: default configuration collapsed 12 sparse changes into 1 chunk; timeout configuration preserved all 12.)
- [x] 12.7 Snapshot worktree and `.git` file hashes before and after the full E2E comparison flow and assert no file/ref/lock/config change; separately run the shipped build invariant checker. (Development fixture SHA-256 stayed `9068cce…c651` through ref/mode/file/theme interactions; fresh production build passed zero-network/zero-write/empty-permission checks.)

## 13. Documentation, manual verification and closure

- [x] 13.1 Document Git comparison in English and Chinese README: five local sources, review/direct semantics, “remote = local snapshot”, zero-network/read-only behavior and explicit unsupported repository shapes; do not advertise AI explanation. (Both READMEs cover the source groups, modes, local-only semantics and repository-shape boundary.)
- [x] 13.2 Add a manual verification checklist for a standard system clone, linked worktree and submodule; record that real FSA can read the standard `.git` and explains authorization-external gitdir cases. (2026-09-04 macOS Chrome gate passed: native FSA read a normal `.git`, linked-worktree and direct-submodule roots showed the explicit authorization-external reason, immutable parent commits rendered the gitlink OIDs, Escape/file-view restoration passed, and the before/after fixture SHA-256 stayed identical. See `docs/git-comparison-verification.md`.)
- [x] 13.3 Run `npm run build`, `npm run check`, the full Git oracle/E2E suite and `openspec validate --all --strict`; resolve every regression without changing existing invariants. (Fresh production build and zero-network/zero-write/empty-permission checks pass; all eight Git oracle/service scripts, hostile-state browser E2E, UI negative controls, `git diff --check`, and strict validation of all 10 OpenSpec items are green.)
- [x] 13.4 Record final main/Git chunk min+gzip, total `dist` and release zip deltas against task 1.1, plus peak worker memory for the large-pack fixture. (Final: main 1,071,039 / 383,566 B gzip-9, delta +10,750 / +3,264; lazy Git UI 37,782 / 13,807; Git worker 48,030 / 17,174; total files 1,550,921 B, delta +108,756 (`du` 1,552 KiB, +112 KiB); web-store-layout zip 556,032 B, delta +36,004. The large-pack path now tracks pack-index arrays, decoded LRU and nested-delta transient buffers together: peak 39,710,220 B, with 32,005,197 B retained in the 33,554,432 B LRU and 3,884 B in pack indexes at completion.)
- [x] 13.5 Capture final light/dark screenshots of ref picker open, SHA validation and unified diff states, and present them to the user before archive/release decisions. (Evidence was presented in the implementation conversation; archive remains intentionally unrequested.)

## 14. Stable project view shell and retained Git session

- [x] 14.1 Reuse the existing project sidebar width, bounds and persistence for the Git change navigation; add the same drag resizer and verify switching between “文件” and “变更” does not change the left-column geometry. (`GitComparison` receives the existing `cv-sidebar-width` state and resize handler. Browser verification dragged the change nav from 280 to 360 px; the file sidebar and restored change nav both measured exactly 360 px.)
- [x] 14.2 Keep `GitComparison`, its diff DOM and its worker mounted after the first entry for the current project; view toggles only change visibility, while project/root exit disposes the session. Add an explicit “重新比较” action that retains endpoints/mode and intentionally rebuilds the result. (The project shell hides the mounted Git host instead of unmounting it. Browser verification retained direct mode, `repo.ts`, endpoints and compare timestamp across both switches; only “重新比较” changed the timestamp, while leaving the project removed the Git host and disposed its component-owned worker.)
- [x] 14.3 Add regression coverage for shared/resizable width, retained endpoints/mode/file/result/diff position without re-probe or re-compare, explicit refresh behavior and project-boundary reset; then run the production build, checks and strict OpenSpec validation. (`check:git-ui-contracts` locks the shared geometry, hidden-not-unmounted session, project-bound cleanup and explicit refresh generation. Browser verification confirmed unchanged compare timestamps across view toggles and a new timestamp only after refresh. `npm run build`, `npm run check`, `git diff --check` and all 10 strict OpenSpec validations pass.)
