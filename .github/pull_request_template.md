<!-- Keep this short. -->

**What changes, and why**

**Invariants** (see CONTRIBUTING.md — check all that you verified)

- [ ] `npm run build && node scripts/check-invariants.mjs` passes
- [ ] Nothing is written to the user's opened directory
- [ ] Where the result is ambiguous or degraded, the UI says so instead of choosing silently

**Behavior change?** If this changes what the extension promises the user, link
the spec change under `openspec/` (or say why the specs are unaffected).

**How you tested it** — including whether `node scripts/e2e.mjs` was run.
