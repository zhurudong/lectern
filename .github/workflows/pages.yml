name: Publish Lectern support and privacy pages
on:
  workflow_dispatch:
    inputs:
      download_url:
        description: 'Verified public HTTPS companion release page; leave empty before release'
        required: false
        type: string
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: false
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
      - run: npm ci
      - run: node scripts/build-public-pages.mjs
        env:
          LECTERN_DOWNLOAD_URL: ${{ inputs.download_url }}
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: release-artifacts/public-site
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
