*[English](PRIVACY.md) · 中文*

# Lectern 隐私政策

更新日期：2026-09-13。

Lectern 提供本地代码阅读器；AI 构建额外提供连接独立安装的伴随程序的可选终端。两者能力不同。Lectern 没有开发者运营的数据收集服务、分析、遥测、广告或账号系统。

## 本地阅读器

阅读、索引、搜索、Markdown 预览、文件和 Git 对比均在本机完成。阅读器不上传项目内容、不获取远程资源、不修改项目文件。所需资源随扩展打包；Markdown 远程图片不会加载。

你通过 Chrome 文件选择器选择文件或目录并授权。另一路径是在扩展详情开启“允许访问文件网址”，授权本地 file URL 访问。Lectern 自动打开设置决定哪些后缀由阅读器打开，不缩小 Chrome 底层授权范围。关闭 Chrome 该开关可撤销 URL 访问，手动选择文件仍可使用。

标准构建使用 storage 保存偏好，declarativeNetRequestWithHostAccess 匹配本地文件导航，host 权限仅 file:///*；没有 HTTP(S) host 权限或内容脚本。

## 可选 AI 终端

只有 AI 构建请求 nativeMessaging。打开终端时，Chrome 按需启动本机 Lectern Companion。扩展向它传递选择的可执行程序、项目标识和显示名、键盘输入、终端尺寸；伴随程序返回所选目录、状态和 CLI 输出。通信在本机完成，不经远程 Lectern 服务器。

伴随程序以你的用户权限启动所选 CLI。**CLI 可以读取、修改文件，并向其配置的模型服务发送提示词、代码或其他上下文。**具体行为由你的操作、CLI 配置和服务商政策决定。阅读器的只读、离线保证不适用于 CLI。Lectern 不管理模型账号、登录、计费或服务商的数据留存。敏感项目应先了解 CLI 和服务商的数据处理规则。Lectern 不注入遥测或直接调用模型 API。

关闭面板会结束连接并请求终止 CLI，不会撤销文件修改或删除 CLI／服务商留存的会话。重新连接会启动新会话。

## 本机存储与删除

- Chrome IndexedDB 保存最近项目句柄和 AI 项目标识，不复制整个项目源码。
- localStorage 保存主题、界面语言、面板宽度和 CLI 可执行程序设置；Chrome 本地存储另保存自动打开偏好与重定向规则。
- 伴随程序在浏览器外的 `~/.lectern-agent/` 保存项目与真实目录的关联。当前 native 传输不使用配对 token；旧开发版可能在该目录留下 token 或日志。
- 终端输出显示在运行中的面板，伴随程序不主动持久化终端记录。所选 CLI 可能自行保存会话、日志或凭据。
- 本地文件网址可能进入 Chrome 历史和会话恢复，由 Chrome 单独管理。

移除扩展会清除其扩展存储和规则，不会卸载伴随程序，也不会清除 CLI、模型服务或 Chrome 历史中的数据。伴随程序需通过其卸载入口单独卸载，默认保留目录关联；卸载后删除 `~/.lectern-agent/` 可清除这些关联。CLI 登录和服务商记录需通过各自工具管理。卸载 Lectern 不删除你的项目文件。

## 下载与外部链接

你点击安装或帮助链接时会打开外部网站，网站和下载服务按自身政策处理请求。Lectern 不在后台下载执行远程扩展代码。Chrome 和操作系统分别管理扩展及安装包更新。

## 联系与验证

源码、支持和隐私问题：[Lectern 项目仓库](https://github.com/zhurudong/lectern/issues)。

标准构建通过 `npm run build && npm run check` 检查，可选 native 边界另有 AI 检查。这些检查不证明第三方 CLI 的行为或隐私实践。
