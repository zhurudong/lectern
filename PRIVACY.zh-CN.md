*[English](PRIVACY.md) · 中文*

# 隐私政策

**Lectern 不收集任何数据、不发送任何数据，也不在你自己的浏览器之外存储任何东西。**
本文档即 Chrome 应用商店 listing 所引用的隐私政策。

最后更新：2026-09-11。

## 收集了什么

没有。没有统计、没有遥测、没有崩溃上报、没有使用数据、没有更新检查、没有授权校验、
没有账号。

按 Chrome 应用商店的数据披露分类，每一项的答案都是"不收集"：个人身份信息、健康信息、
财务信息、身份验证信息、个人通信、位置、浏览历史、用户活动。

## 什么东西离开了你的电脑

没有。标准扩展不连接远程服务、不外传文件内容或路径。它需要的一切 —— 编辑器、语法、
字体 —— 都打包在扩展里；本地 `file://` 读取只发生在本机。

扩展声明 `storage` 来保存自动打开偏好，声明 `declarativeNetRequestWithHostAccess`
来将匹配的本地文件导航转入查看器。主机权限只有 `file:///*`，没有 HTTP(S) 主机权限
或内容脚本；仅将 `viewer.html` 对 `file:///*` 暴露为可访问资源。

## 它能读什么，以及你如何授权

通过 Chrome 自己的文件选择器选择文件夹或文件时，扩展获得所选项目的句柄。这条路径
不需要开启 Chrome 的文件网址权限。

你也可以在 Lectern 的 Chrome 扩展详情里开启**「允许访问文件网址」**，让扩展直接
读取本地文件地址。这个 Chrome 设置授予的本地 URL 访问能力超出了选择器逐项授权的
范围。查看器的「自动打开」设置决定哪些后缀会在当前标签页自动打开，不会缩小底层
Chrome 授权。不接管 HTTP(S)、远程文件主机、目录或网页子资源；关闭 Chrome 开关可撤销
URL 访问。

自动打开把不超过 64 MiB 的单个文件读入内存，刷新标签页时重新读取。它不会获得父目录
句柄，不会索引相邻文件，也不会解析 Markdown 相对资源。更大文件可手动选择；超过
5 MiB 的文本只预览前 1 MiB。

它**从不写入**。通过 File System Access API 写文件必须经 `createWritable()`，而该
符号在本扩展的源码与构建产物中**均不存在** —— 一旦出现，项目 CI 就会失败。

## 存了什么，存在哪

存在你自己机器上、你自己的 Chrome 配置里：

- **IndexedDB** —— 最近项目的句柄（便于重启后重连），以及展示用的名称与路径；
- **`localStorage`** —— 界面偏好：主题、侧栏宽度、面板折叠状态；
- **`chrome.storage.local`** —— 自动打开总开关与选中的文件后缀，不同步到远程账号。

Chrome 还会在本机保存扩展的重定向规则，内容是选中的后缀，不是打开文件的历史。自动
打开的文件地址会进入查看器标签页的 URL，可能由 Chrome 自己的历史或会话恢复功能
保留；Lectern 不会上传它。

Lectern 不会传输这些数据。卸载扩展会移除它保存的句柄、偏好与规则；Chrome 的浏览
历史需单独管理。**你的项目文件从不会被复制进扩展存储** —— 里面只有 Chrome 发给
扩展的句柄、偏好和重定向规则。

## 第三方

没有。没有 SDK、没有 CDN、没有从字体服务加载的字体、没有远程图片（Markdown 里引用
远程 URL 的图片会被**有意替换成占位符**而不是去拉取）。

## 怎么验证以上所有

你不必相信这份文档。扩展按 Apache 2.0 开源，自己构建并跑一遍不变量检查：

```bash
npm ci && npm run build
node scripts/check-invariants.mjs
```

检查约束精确权限与 CSP、唯一审计读取模块的固定摘要、非本地地址拒绝，以及其余产物
不得出现传输 API。所有产物仍禁止文件写入 API。同样的检查在每次提交的 CI 里都会跑，
**过不了检查的构建永不发布**。

## 联系方式

在项目仓库开 issue。
