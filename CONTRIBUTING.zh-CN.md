*[English](CONTRIBUTING.md) · 中文*

# 参与贡献

先谢谢你看到这儿。在动手之前请先读下面三条不变量 —— 它们是这个项目存在的理由，
**破坏其中任何一条的改动都不在这个项目的范围内，无论写得多好。**

## 三条不变量

**1. 无远程传输。** 标准扩展不连接远程服务，不外传文件内容和路径。无遥测、无更新
检查、无远程字体、无 CDN、无统计、也没有"匿名使用数据"。它需要的一切都在包里。
本地 `file://` 读取仅允许在 Chrome 文件网址授权后，通过审计的 URL 读取模块进行。

这一条是可检查的，而且检查本身就是承诺的一部分：

```bash
npm ci && npm run build
node scripts/check-invariants.mjs
```

`scripts/check-invariants.mjs` **唯一维护**禁用符号清单与精确的权限、CSP 白名单，CI
运行相同检查。传输 API 仅可出现在唯一审计模块 `local-file-reader.js`，模块精确内容
由固定摘要校验。修改它时必须重新审阅读取行为并更新审计，覆盖 HTTP(S)、远程文件主机
和无效地址的负向检查；只改摘要不算完成审计。

标准构建只允许 `storage` 与 `declarativeNetRequestWithHostAccess`，主机范围仅
`file:///*`；仅将 `viewer.html` 对 `file:///*` 暴露为可访问资源。CSP 为
`script-src 'self'; object-src 'self'; connect-src 'self' file:`。地址验证、禁止
重定向与 64 MiB 自动读取上限必须守在读取入口，渲染内容不得触发任意本地读取。后缀
配置控制顶层文件导航，不改变 Chrome 底层授权范围；不得扩展成 HTTP(S)、目录或子资源
接管。

注意它检查的对象：**是 `dist/` 里的构建产物，不是仓库。** `scripts/` 下的验收基建
会正当地写文件（它在 OPFS 里造合成项目），那不算违规 —— 不变量约束的是**发出去的
东西**，不是测试装置能做什么。**把仓库本身弄成"符号干净"，等于为了让检查通过去改
被检查的对象。**

**2. 只读。** 扩展绝不往用户打开的目录里写任何东西 —— 不写锁文件、不写索引缓存、
不留标记。所有持久状态都在用户自己 Chrome 配置目录下的浏览器存储里（IndexedDB、
`localStorage`、`chrome.storage.local`）。File System Access API 要写文件必须经 `createWritable()`，
该符号在 `src/` 下从不出现，一旦进入产物 CI 就会失败。

这两条**是一对**，而这对就是产品本身。当前约束包含明确审计的本地读取例外，不再依赖
所有读取或传输 API 符号都不存在。边界变化时，代码、检查与权限披露必须保持一致。

**3. 决定权属于用户。** 这个工具对你代码的一切认知都是**启发式**的 —— 跳转不做类型
推断、不做导入解析，索引基于符号名，覆盖度因语言而异。**一个启发式工具替用户做决定，
错的时候是静默的，用户永远不会知道自己被坑了。** 所以：

- 有多个同样合理的答案时（两处同名定义），**给列表**，不要排个序就跳过去；
- 能力降级时（文件过大、语言不参与索引、索引未完成），**在界面上说出来**，不要静默降级；
- 状态丢失或项目关联断开时，**给用户一条修复路径**，而不是甩一个空白面板。

## 这里的改动是怎么发生的

本项目 spec 驱动。行为变更先以规格的形式提出与评审，位于 [`openspec/`](openspec/)：

- `openspec/specs/` —— 当前已批准的行为；
- `openspec/changes/` —— 在飞的提案（proposal、design、delta spec、tasks）。

修 bug 或明显正确的小改动，直接开 PR。**任何改变"扩展对用户的承诺"的改动**，请先开
issue 说明行为变化；**规格是契约，它与代码一起更新，而不是事后补。**

一条贯穿规格、README 与界面文案的写法：**说边界，不说欠条。** "不解码 UTF-8 以外的
编码"是边界；"目前只支持 UTF-8，其他编码以后补"是一张得一直还的欠条。另外，任何
"不覆盖"的清单都必须注明**是举例而非穷举** —— 否则某个语言没出现在清单里，会被读成
"那大概支持"。

## 开发

```bash
npm ci
npm run build       # 类型检查 + 产物构建到 dist/
npm run dev         # watch 构建
npm run typecheck
```

装载扩展：`chrome://extensions` → 开启「开发者模式」→「加载已解压的扩展程序」→ 选
`dist/`。需要 Chrome 122+。

验收本地自动打开时，在 Chrome 扩展详情开启「允许访问文件网址」，再从查看器顶栏
「自动打开」配置后缀。已授权和未授权两种状态都要验证，原生选择器必须在两种状态下
都能使用。自动打开保留单文件能力，刷新标签页时重新读盘，不获得父目录句柄。

端到端验收（真实 Chrome + 真实扩展 + OPFS 合成项目）：

```bash
node scripts/e2e.mjs
```

它是长跑（数百条断言，约十分钟），并且会开一个可见的 Chrome 窗口。**把输出重定向到
文件再读文件** —— 用 `grep`/`head` 接管道会因缓冲一行不吐，健康的运行看起来和卡死
一模一样。

拖放打开的专项回归运行 `node scripts/e2e-drop.mjs`，使用真实本地文件和目录，
生产构建与浏览器配置均放在临时目录中。

## 增加语言覆盖

[`docs/language-support.md`](docs/language-support.md) 写明了每一档的含义，以及新增
一门语言需要提供什么。两处最容易做错：

- 覆盖度是**分能力**的（高亮、大纲、跳转、引用），同一门语言在不同能力上落在不同档
  位是正常的 —— **不要把它们压成一个标签**；
- 每一份覆盖清单都要一起改 —— 两版 README、`docs/language-support.md` 与规格。
  `node scripts/check-language-coverage.mjs` 会把它们与实现逐项对账，任一处不符即失败；
- 自动打开候选必须来自预览格式表，不纳入不支持预览的二进制，HTML 和图片默认不选，
  `Dockerfile` 等完整文件名规则与后缀规则分开处理；
- 在合成样本上跑通的语法规则，在真实代码上经常误收。**声称某一档之前，请拿该语言的
  真实开源仓库跑一遍，并说明你拿什么跑的。**

## 贡献的许可

提交贡献即表示你同意你的贡献按 [Apache License 2.0](LICENSE) 授权，与项目一致。
无 CLA。
